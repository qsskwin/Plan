# pytest 失败测试学习记录

- 日期：2026-08-18
- 测试：`test_rotation_matrix_maps_x_to_y`
- 制造失败的方法：把正确期望值 `vector_y` 临时改为 `np.array([0.0, -1.0, 0.0])`。
- 断言位置：`test_vector_operations.py:27`（失败运行时的行号）。
- 实际值：`[0.0, 1.0, 0.0]`
- 错误期望值：`[0.0, -1.0, 0.0]`
- 报告含义：`Mismatched elements: 1 / 3` 表示三个分量中有一个不匹配；最大绝对差为 `1 - (-1) = 2`。
- 原因：旋转矩阵将 x 轴正方向映射到 y 轴正方向，故意写入的 y 轴负方向期望值错误。
- 修复：将期望值恢复为 `vector_y`，随后重新运行完整测试集。
- I/H/G：`G`（本记录由 Codex 根据本人运行得到的失败输出直接整理，不计入独立掌握）。
