# 第 04 周进度汇报

> - 状态：已结束（控制项目门槛通过；算法题限时与关键不变量正式验收按本人决定跳过）
> - 周期：2026-08-31 至 2026-09-06
> - 整理日期：2026-09-11
> - 总体路线：[总体规划](../../总体规划.md)
> - 本周计划：[第 04 周执行计划](./第04周计划_2026-08-31至2026-09-06.md)
> - 项目目录：[aerial-control-lab](../../aerial-control-lab/)
> - 执行说明：实际从 2026-09-04 开始按原顺序补做，不因实际日期较晚压缩任务；跳过项、未验证项和 `G` 来源均如实保留。
>
> 来源标记：
>
> - `I`：Independent，独立完成；
> - `H`：Hint，在提示或讲解后自行完成；
> - `G`：Generated，由 Codex 直接生成，不计入个人独立掌握。

## 1. 总体完成度

- 自评完成度：本人未单独给出百分比；按计划证据，第四周控制项目门槛已通过，算法题正式限时/解释验收跳过 90%的自评。
- 周一至周六实际有效投入：未单独统计
- 是否使用周日突击：否；周日只恢复并确认资料已保存，不新增功能
- Windows 验收：通过；在新目录完成全量构建，当前版本 CTest `100/100`
- Python 命令行验收：通过；固定 `D:\anaconda\python.exe` 执行当前版本全量 pytest，结果为 `124 passed in 0.43s`
- 三维质点平动门槛：通过；Python/C++ 模型、三种基础工况、质量偏差、解析对照、跨语言比较、正式测试收集和周末回归均有证据
- 掌握度边界：核心平动实现包含 `H`，统一演示、批量边界测试、跨语言报告和部分解释包含 `G`；工程通过不等于所有代码均能独立重写

## 2. 每日完成情况

### 2026-08-31（周一）

- 状态：完成；跳过项和未验证项保留
- 已完成：
  - 确认周初基线：C++ 全量 CTest `84/84`，Python 全量 pytest `99/99`，固定解释器为 `D:\anaconda\python.exe`，当时 HEAD 为 `0e17b8b`；命令由 Codex 执行，只作为环境证据；
  - 在公式提示后分别完成 Python/C++ 最小 RK4 核心重写，两端与正式实现的最终状态一致，跨实现最大绝对差为 `0`，相对简谐振子解析解的最大绝对误差均约为 `6.61249e-07`；Python 核心和 C++ 四阶段核心记为 `H`，C++ 解析、汇报和函数整理等局部记为 `G`；
  - 独立完成 NED→ENU→NED 方向推导和 `R_NB^T=R_NB^{-1}` 说明；正滚转推力方向在约定提示后完成，周一坐标与推力方向整体记为 `H`；
  - 由 Codex 生成最小 Python 数值验证脚本，验证 NED/ENU 往返、模长、`R_NB^T` 恢复和正滚转推力方向，数值误差分别为 `0`、`0`、约 `4.44089e-16` 和约 `1.11022e-16`，脚本记为 `G`；
  - 将 `oscillatorErroeulerStepr` 的误拼名称更正为 `oscillatorError`，仅修改 1 处定义和 6 处调用，积分器相关 CTest `17/17`，本项记为 `H`；
  - 438. 找到字符串中所有字母异位词：本人报告已 AC，当日自评 A。
- 未完成：Week4 坐标方向 GoogleTest 按本人决定跳过；438 的固定窗口边界、进出窗口更新和频次数组相等解释未验收
- 主要阻塞：曾混淆 RK4 采样状态与状态导数，并在 Eigen 动态向量构造和时间参数上出现编译问题；经提示后修正
- 当天 I/H/G：坐标往返与逆变换说明 I；最小 RK4 核心 H；坐标方向整体 H；数值验证脚本 G；小范围重命名 H；438 仅保留用户自评

### 2026-09-01（周二）

- 状态：完成；算法解释验收跳过
- 已完成：
  - 完成六维状态 `[p_n,p_e,p_d,v_n,v_e,v_d]`、导数结构、单位姿态自由落体、水平悬停和固定正滚转推导；整体为 `H`，其中零推力自由落体方向判断为 `I`；
  - 新增 `python/aerial_control/point_mass.py`，在接口模板和分步提示后完成 `point_mass_derivative`，复用四元数 B→N 旋转，加入状态、质量、推力和有限性检查；核心实现记为 `H`；
  - 修正首版 NED 重力符号错误，并补齐 NaN/Inf 拒绝；定向结果为自由落体加速度 `[0,0,9.80665]`、悬停 `[0,0,0]`、固定正滚转 `[0,1.72917699,0]`；
  - 完成 `python/tests/test_point_mass.py` 的正式行为覆盖，定向结果为 `25 passed in 0.15s`。位置导数、自由落体、悬停、质量/推力/状态非法输入和输入不变性为 `H`；正滚转最终修正及两类四元数测试含 `G`，整体为 `H/G`；
  - 15. 三数之和：本人报告已 AC，自评 B。
- 未完成：15 的排序、双指针移动、三处去重和复杂度解释按本人决定跳过；本日只做 Python 定向测试，没有执行全量回归
- 主要阻塞：首版将 NED 重力写成负号；正滚转测试两次混淆实际角 `phi` 与四元数半角 `phi/2`，并一度未同步调整补偿推力
- 当天 I/H/G：六维推导 H；Python 平动核心 H；Python 测试 H/G；15 仅保留用户自评和“解释未验证”边界

### 2026-09-02（周三）

- 状态：完成
- 已完成：
  - 新增通用固定步长入口 `simulate_fixed_step`，本人在下标和 API 提示后完成整数索引 RK4 核心循环，记为 `H`；Codex 直接补齐输入校验、整除检查、预分配和时间数组等支撑部分，文件整体为 `H/G`；
  - 完成自由落体、水平悬停和固定正滚转三条 Python 完整轨迹，并逐时刻、逐分量与独立解析轨迹比较；三个工况主体为 `H`，复用的结构断言和输出框架含 `G`；
  - 三个工况分别得到 201、1001、201 个状态；最大位置/速度误差依次为 `2.132e-14 m / 3.908e-14 m/s`、`0 / 0`、`9.326e-15 m / 1.332e-14 m/s`；
  - Codex 直接生成三工况可视化脚本与三张四面板 PNG，完整轨迹断言均通过；当周记录已检查轨迹、标题、单位、图例、误差线与裁切情况，可视化记为 `G`；
  - Codex 直接整理 `四元数、旋转矩阵、坐标系转换.md`，记为 `G`；源码结构检查通过，但未记录实际 Markdown 数学预览验收；
  - 21. 合并两个有序链表：本人报告已 AC，自评 C。
- 未完成：21 的哑节点、尾指针不变量和边界解释未验收；本日脚本运行不等于正式 pytest 或全量回归
- 主要阻塞：正滚转解析轨迹首版把 `T sin(phi)` 的力直接当作加速度，遗漏除以质量；经单位提示后修正
- 当天 I/H/G：仿真循环 H/G；三个工况主体 H、支撑框架 G；可视化与概念笔记 G；21 仅保留用户自评

### 2026-09-03（周四）

- 状态：完成；本日结果为定向测试，不是全量回归
- 已完成：
  - 新增 `cpp/include/core/point_mass.hpp` 和 `cpp/src/point_mass.cpp`，接入根 `aerial_core` target；本人在接口、Eigen API 和 CMake 提示后完成六维动力学核心，记为 `H`；Codex 只直接整理局部格式，相关整理记为 `G`；
  - 完成位置导数、自由落体、悬停和固定正滚转等首批代表性 GoogleTest，主体为 `H`，首个测试最后局部修正含 `G`；
  - Codex 按本人明确要求补齐质量/推力/状态/四元数边界、输入不变性、非单位四元数归一化和三条完整 RK4 轨迹测试，新增部分记为 `G`；
  - 定向构建 `aerial_core_gtest` 成功，限定运行 `PointMassDerivativeTest.*`，实际收集并通过 `16 tests from 1 test suite`；
  - 明确 `T=0` 是自由落体的合法输入，负推力测试必须使用 `T<0`；
  - 141. 环形链表：本人报告已 AC，当日自评 C。
- 未完成：141 的快慢指针相遇原因及四类边界解释未验收；没有运行其他 GoogleTest、CTest、pytest 或全量回归
- 主要阻塞：初始阶段混淆头文件声明与函数定义、命名空间和匿名命名空间作用域；非法推力边界一度误认为应使用 `T=0`
- 当天 I/H/G：C++ 平动核心 H；首批代表性测试 H/G；批量边界与完整轨迹测试 G；141 仅保留用户自评

### 2026-09-04（周五）

- 状态：完成；全量回归留到周六
- 已完成：
  - 建立 Python 统一演示。工况配置骨架、三个解析分支、仿真调用、误差分离和 CSV 保存等前期工作含 `H`；Codex 直接补齐其余工况配置、最终汇报和 `main`，整体为 `H/G`；
  - Codex 直接新增 C++ `point_mass_demo`、局部 CMake 接入和最终双语言收口，记为 `G`。两端统一使用 NED/FRD、`q_NB` 的 B→N 方向、非负标量推力及 FRD 力向量 `[0,0,-T]`；
  - Python/C++ 均运行自由落体、水平悬停和固定正滚转，分别保存 201、1001、201 个状态的 19 列完整 CSV，三工况数值终点和最大误差一致；
  - Codex 直接生成跨语言比较脚本和报告，逐时刻、逐分量完成 Python→解析、C++→解析和 Python↔C++ 三类比较；三个工况的 Python↔C++ 最大位置/速度误差均为 `0`，本项记为 `G`；
  - 质量增加 10% 实验在运行前由本人独立预测 Down 方向和恒加速度终点结构，记为 `I`；Codex 纠正“合力误作加速度”的量纲错误并完成双语言实现、比较和报告，相关部分记为 `G`；
  - Codex 更新根 README，补充状态、坐标、推力、模型假设、运行命令、误差和第五周反馈入口，记为 `G`；Git 差异使用命令行只读核对，未能在当时工具条件下完成 VS Code Source Control 的实际 GUI 可视检查；
  - 234. 回文链表：本人报告已 AC，自评 B。
- 未完成：234 的找中点、反转后半链表、比较和是否恢复结构的解释未验收；本日未执行 pytest、GoogleTest、CTest 或全量回归
- 主要阻塞：质量偏差预测首版把 `1.65g-14.709975` 的合力直接当作加速度；Python CLI 帮助字符串中的 `%` 需使用 `%%` 转义，已修正
- 当天 I/H/G：Python 统一演示 H/G；C++ 演示和 CMake G；跨语言比较 G；质量偏差预测 I、实现与验证 G；README G；234 仅保留用户自评

### 2026-09-05（周六）

- 状态：已结束；任务 A—D、F 完成，任务 E 正式验收按本人决定跳过
- 已完成：
  - 在此前不存在的新目录 `build/week4-sat-clean-20260910` 完成 Windows MinGW Debug 配置与全目标构建；受限网络首次无法下载 Eigen，联网重试后成功。当前版本 CTest `100/100`，失败 `0`，其中 `PointMassDerivativeTest.*` 16 项全部收集并通过；
  - 使用固定解释器完成 Python 全量回归，结果为 `124 passed in 0.43s`，失败 `0`、跳过 `0`；`python/tests/test_point_mass.py --collect-only -q` 确认收集 25 项；
  - 审计同一提交版本的 8 份 19 列 CSV 和两份比较报告，确认四个工况的样本数、终点、解析误差和跨语言误差；没有无目的重跑周五演示；
  - 完成五组简短解释验收：六维状态与 B→N 数据流为 `H`；三个固定工况为 `I/G`；增重与开闭环恢复为 `I`；完整 6-DOF 缺失状态为 `H/G`；解析/跨语言对照与 RK4 收敛性为 `G/H`；
  - 本人报告累计 14 道 LeetCode 均已重新完成并给出最新统一自评；未提供逐题新 AC、用时、提示来源或关键不变量证据，因此不追加推断；
  - 核对本周提交链，并由本人完成周六提交 `78fe40b 20260905Sat4`。任务 F 开始前正式工作区干净，Codex 未执行 stage、unstage、commit、discard 或清理。
- 未完成：算法题限时实现和关键不变量正式验收按本人决定跳过，不补记通过；相关题目进入第五周复习队列
- 主要阻塞：无工程阻塞；唯一构建失败来自受限网络下的 Eigen 依赖获取，不是源码或 CMake 回归
- 当天 I/H/G：构建、测试、审计和周末汇总由 Codex 执行或整理；解释验收按各小项保留 I/H/G；算法题只记录本人报告与自评，不推断来源

## 3. I/H/G 统计

| 核心成果 | I/H/G | Codex 提供的帮助 | 能否从空文件重写 |
|---|---|---|---|
| Python 最小 RK4 重写 | `H` | 提示四阶段公式、状态与导数区别，并检查正式实现对比 | 已从空文件完成最小核心；完整独立复验未再次进行 |
| C++ 最小 RK4 重写 | `H/G` | 核心四阶段经提示完成；解析解、标签化输出、函数层次和格式由 Codex 补齐 | 四阶段核心已有 H 证据；完整演示包装不是独立重写 |
| 坐标与推力方向 | `I/H` | NED/ENU 往返与 `R_NB^T` 说明由本人独立完成；正滚转方向经约定提示完成 | 概念可解释；最小自动化验证脚本为 G |
| Python `point_mass_derivative` | `H` | 提供接口模板、坐标/符号提醒、有限性检查和错误定位 | 已在提示后完成正式核心，未单独再次从空文件复写 |
| Python 平动测试 | `H/G` | 本人完成主要物理与参数边界；Codex 修正正滚转并生成四元数测试 | 主要测试有 H 证据；生成部分不计入独立掌握 |
| Python 固定步长仿真 | `H/G` | 本人完成整数 RK4 循环；Codex 补齐校验、整除检查、预分配和时间数组 | 核心循环有 H 证据；完整支撑代码未独立重写 |
| Python 三个基础轨迹 | `H/G` | 本人完成工况与解析轨迹；Codex 提供通用断言和输出框架 | 工况主体有 H 证据；整体文件不是纯独立实现 |
| Python 可视化与概念笔记 | `G` | Codex 直接生成绘图脚本、PNG 和坐标/四元数笔记 | 否，不计入独立掌握 |
| C++ 三维质点平动核心 | `H` | 提供声明/定义、Eigen、命名空间和 CMake 小步骤提示，并做局部格式整理 | 核心已有 H 证据；未再次从空文件复写 |
| C++ 平动测试 | `H/G` | 首批代表性物理测试由本人经提示完成；批量边界、输入不变性和完整轨迹由 Codex 生成 | 代表性测试可在提示后完成；生成部分不计入掌握 |
| Python 统一演示 | `H/G` | 本人完成解析、仿真、误差和保存等主要步骤；Codex 补齐配置、汇报与主入口 | 部分可重写；完整程序无纯 I/H 证据 |
| C++ 统一演示与跨语言比较 | `G` | Codex 直接生成 C++ demo、CMake 接入、比较脚本和报告 | 否，不计入独立掌握 |
| 质量增加 10% 实验 | `I/G` | 本人独立预测方向和运动学结构；Codex 纠正量纲并完成实现、比较和报告 | 物理方向判断可独立完成；实现与报告无独立重写证据 |
| 周六物理解释验收 | `I/H/G` | 按问答纠正精确符号、完整刚体状态、两类对照和 RK4 收敛性 | 增重与开闭环判断为 I；其余按小项保留 H/G 边界 |
| README、报告与周报整理 | `G` | Codex 直接整理工程说明、比较报告和最终周报 | 否，不计入独立掌握 |
| 干净构建与全量回归执行 | `G`（操作来源） | Codex 执行构建、测试与结果审计 | 运行结果是工程证据，不作为本人独立操作证据 |

## 4. 数学、坐标系与物理验收

| 项目 | 通过/部分/未通过 | 主要问题与证据边界 |
|---|---|---|
| RK4 四阶段概念 | 通过（H） | 能在提示后写出起点、两个预测中点和预测终点的采样关系，并区分采样状态与状态导数 |
| Python/C++ 最小 RK4 核心 | 通过（H） | 双语言核心均已从空文件完成并与正式实现一致；C++ 外围整理含 G |
| NED/ENU 与逆变换 | 通过（I） | 独立给出 `[1,2,3]→[2,1,-3]→[1,2,3]`，并用正交性说明 `R_NB^T=R_NB^{-1}` |
| 六维状态与 B→N 数据流 | 通过（H） | 状态顺序、单位和总体数据流可说明；FRD 推力、NED 重力的精确符号和导数单位在提示后补齐 |
| 三个固定工况 | 完成（I/G） | 自由落体、悬停和正滚转方向判断为 I；正滚转完整分量补偿和解析加速度由 Codex 补全为 G |
| 质量偏差与开闭环恢复 | 通过（I） | 独立判断增重且推力不变会向 Down 加速，并说明固定前馈不能自恢复、需要反馈闭环 |
| 当前模型与完整 6-DOF | 部分通过（H/G） | 能指出当前姿态未随动力学积分；新增姿态、角速度状态及角加速度定义的精确说明含 G |
| 解析对照与跨语言对照 | 部分通过（G） | 两类对照的独立作用由 Codex 直接说明，尚无本人独立解释证据 |
| RK4 四阶收敛判断 | 通过（H） | 经提示说明应使用简谐振子等非平凡系统，并检查步长减半后 `E(h)/E(h/2)≈16` |
| Week4 坐标方向 GoogleTest | 跳过/未验证 | 周一按本人决定跳过；后续平动正式测试覆盖不能回写成该计划项的独立完成证据 |

当前模型的统一约定：

- 状态为 `x=[p_n,p_e,p_d,v_n,v_e,v_d]`，位置单位 m、速度单位 m/s；
- NED 重力为 `g^N=[0,0,+g] m/s²`，FRD 推力为 `f_T^B=[0,0,-T] N`，且 `T>=0`；
- `q_NB/R_NB` 将机体系 B 向量旋转到 NED 世界系 N；
- 平动加速度为 `a^N=g^N+(1/m)R_NB f_T^B`，状态导数为 `[v_n,v_e,v_d,a_n,a_e,a_d]`；
- 当前模型只有三个平动自由度。完整刚体模型还需要姿态运动学、机体系角速度状态、力矩输入和刚体转动动力学。

## 5. 数值实验结果

四个工况统一使用 `dt=0.01 s`、double、整数步索引和 RK4，保存初态与终点。下表中的状态顺序均为 `[p_n,p_e,p_d,v_n,v_e,v_d]`。

| 工况 | 初始/终止时刻 | 更新次数/状态数 | 质量、姿态与推力 | 解析终点 | Python/C++ 数值终点 |
|---|---|---|---|---|---|
| 自由落体 | `0 / 2 s` | `200 / 201` | `m=1.5 kg`，`q_NB=[1,0,0,0]`，`T=0 N` | `[0,0,19.6133,0,0,19.6133]` | `[0,0,19.613299999999985,0,0,19.613300000000038]` |
| 水平悬停 | `0 / 10 s` | `1000 / 1001` | `m=1.5 kg`，`q_NB=[1,0,0,0]`，`T=14.709975 N` | 六维零状态 | 六维零状态 |
| 固定正滚转 | `0 / 2 s` | `200 / 201` | `m=1.5 kg`，`q_NB=[cos(5°),sin(5°),0,0]`，`T=14.936900075174012 N` | `[0,3.4583539707293358,0,0,3.4583539707293358,0]` | `[0,3.4583539707293451,0,0,3.4583539707293491,0]` |
| 质量加 10%，推力不变 | `0 / 2 s` | `200 / 201` | `m=1.65 kg`，`q_NB=[1,0,0,0]`，`T=14.709975 N` | `[0,0,1.7830272727272707,0,0,1.7830272727272707]` | `[0,0,1.7830272727272736,0,0,1.7830272727272753]` |

完整轨迹比较统一使用 `rtol=0`、位置 `atol=1e-10 m`、速度 `atol=1e-10 m/s`。每格按“最大位置误差 m / 最大速度误差 m/s”记录。

| 工况 | Python→解析 | C++→解析 | Python↔C++ | 结论 |
|---|---|---|---|---|
| 自由落体 | `2.1316282072803006e-14 / 3.907985046680551e-14` | `2.1316282072803006e-14 / 3.907985046680551e-14` | `0 / 0` | 通过 |
| 水平悬停 | `0 / 0` | `0 / 0` | `0 / 0` | 通过 |
| 固定正滚转 | `9.3258734068513149e-15 / 1.3322676295501878e-14` | `9.3258734068513149e-15 / 1.3322676295501878e-14` | `0 / 0` | 通过 |
| 质量加 10%，推力不变 | `2.886579864025407e-15 / 4.6629367034256575e-15` | `2.886579864025407e-15 / 4.6629367034256575e-15` | `0 / 0` | 通过 |

- 正滚转 `phi=10°` 时，`a_e=g tan(phi)=1.729176985364668 m/s²`，Down 分量由 `T cos(phi)=mg` 抵消；
- 质量偏差工况的正确加速度为 `a_d=(1.65g-14.709975)/1.65=0.8915136363636364 m/s²`；本人原式 `1.65g-14.709975` 表示合力，不是加速度；
- 解析对照用于判断每端是否符合独立物理关系，跨语言对照用于发现两套实现是否不一致，二者不能互相替代；
- 当前恒加速度工况适合验证模型接入、方向、时间网格和解析轨迹，但不适合展示 RK4 四阶误差缩放；后续应使用简谐振子等非平凡系统检查 `E(h)/E(h/2)≈16`；
- 当周记录已运行并检查 `python/exercises/Week4/Wed/task_C/outputs/free_fall_trajectory.png`、`hover_trajectory.png` 和 `positive_roll_trajectory.png`，轨迹、单位、图例、误差与容差线无明显异常；绘图实现来源为 `G`。

## 6. 工程结果

- Windows 周六干净构建目录：`aerial-control-lab/build/week4-sat-clean-20260910`
- 构建环境：MinGW GNU C++ 15.2.0、CMake 4.4.2、Eigen 3.4.1、GoogleTest 1.17.0
- C++ 周初基线：CTest `84/84`
- C++ 当前全量结果：CTest `100/100`，失败 `0`，约 `1.29 s`
- C++ 平动定向结果：`PointMassDerivativeTest.*` 共 `16/16`；该数字不是全量 CTest 结果
- Python 周初基线：pytest `99/99`
- Python 当前全量结果：`124 passed in 0.43s`，失败 `0`、跳过 `0`
- Python 平动测试：`python/tests/test_point_mass.py` 共收集 25 项；周二定向运行 `25 passed in 0.15s`
- 项目源码警告：未发现；配置阶段仅有 Eigen 3.4.1 上游 `CMP0146` deprecated author warning
- 统一演示产物：Python/C++ 四个工况共 8 份 19 列 CSV；自由落体、正滚转和质量偏差各 201 个样本，悬停 1001 个样本；另有两份跨语言比较报告和三张基础工况 PNG
- 本周提交链：`67ba452 20260831Mon4`、`b1d3949 20260901Tue4`、`0f313d1 20260902Wed4`、`3a11d45 20260903Thu4`、`a1f5272 20260904Fri4`、`78fe40b 20260905Sat4`
- 相对第四周开始前的 `0e17b8b 20260830_week3_done`，截至 `78fe40b` 共涉及 42 个文件、增加 8457 行、删除 950 行
- 周六任务 F 开始前正式工作区干净；本次重新整理 `week4_done.md` 后，该文件应作为合法未提交修改由本人按自己的节奏处理，Codex 不自动提交或丢弃

## 7. Hot 100

| 题目 | 本周记录 | 最新等级 | 核心不变量/解释验收 | 主要问题 |
|---|---|---|---|---|
| 438. 找到字符串中所有字母异位词 | 周一报告已 AC | B（周末统一自评） | 未验收 | 26 字母频次数组、固定窗口与进出窗口更新仍需巩固 |
| 15. 三数之和 | 周二报告已 AC | B（周末统一自评） | 未验收 | 排序、双指针移动和三处去重解释跳过 |
| 21. 合并两个有序链表 | 周三报告已 AC | C（周末统一自评） | 未验收 | 哑节点、尾指针不变量和边界处理需要优先复习 |
| 141. 环形链表 | 周四报告已 AC | B（周末统一自评） | 未验收 | 快慢指针相遇原因及空链表/单节点边界未解释 |
| 234. 回文链表 | 周五报告已 AC | B（周末统一自评） | 未验收 | 找中点、反转后半链表、比较及结构恢复未解释 |

周末统一复盘：

- 本人报告累计 14 题均已重新完成，但未提供逐题新的 AC、实际用时、提示来源或关键不变量证据，因此不额外推断 I/H/G；
- 最新等级 A：1、49、128、283、11、3、560；
- 最新等级 B：15、438、160、234、206、141；
- 最新等级 C：21；
- 第五周优先复习 21，其次轮换 15、438、160、234、206、141；
- 周六任务 E 的限时实现和关键不变量正式验收按本人决定跳过，状态保持“跳过”，不补记为通过。

## 8. 本周最有价值的三个产出

1. 建立了 Python/C++ 三维质点平动模型和统一演示，用自由落体、水平悬停、固定正滚转及质量偏差四个工况完成逐时刻解析验证与跨语言对照。
2. 将 NED/FRD、`q_NB` 的 B→N 映射、推力方向、质量归一化和六维状态导数连成完整物理数据流，并由质量偏差实验自然引出固定前馈与闭环反馈的区别。
3. 学习了Python和C++的新的语法，比如函数包装器等，还有numpy和eigen的库运用。

## 9. 最大的三个阻塞

1. 对C++和python的手动代码编写还是有一些问题，需要依赖提示才能完整写出来，不能依靠自己全部写出来，不过现在AI很强，不知道是否还需要进一步深化。不过能全部看懂。
2. 对于C++和python的eigen语法和numpy的语法运用的还不是很熟悉，经常弄混淆。
3. 算法题虽然已有重做和自评记录，但本周计划内的限时、关键不变量与边界解释验收被跳过；21 为 C，六道题为 B，仍需按队列复习。

## 10. 第五周进入判断

- 是否可以进入第五周：可以；第四周控制项目门槛已经通过
- 已通过的门槛：Python/C++ 平动核心、三种基础工况、质量偏差、独立解析比较、跨语言比较、正式测试收集、Windows 干净构建和双语言全量回归
- 仍需保留的能力边界：统一演示与批量测试中的 `G`、完整 6-DOF 精确定义中的 `G`、解析对照与跨语言对照独立作用的 `G`，以及未验收的算法题关键不变量
- 第五周控制入口：在现有平动模型上引入竖直位置/速度反馈；可使用 `p_d/v_d`，或定义高度 `h=-p_d`、`h_dot=-v_d`，但参考量、误差符号和推力方向必须保持一致
- 第五周算法入口：优先复习 21，再轮换 15、438、160、234、206、141；A级题暂不列为优先任务

## 11. 希望第五周如何调整

- 不重复整周已经通过的构建与轨迹工作；从最小竖直反馈闭环开始，把质量偏差下“为什么不能恢复”转化为可观察的控制效果。
- 对本周关键 `G` 内容采用小范围独立复写或口头复验，优先巩固完整 6-DOF 状态、解析对照与跨语言对照的区别。
- 使用简谐振子等非平凡系统做多步长实验，验证 RK4 全局误差在步长减半时约缩小到原来的 `1/16`，避免仅用恒加速度工况判断阶数。
- 按 C→B→A 的顺序复习算法题；至少补齐 21 的链表不变量，并轮换六道 B 级题的关键不变量和边界解释。
- 尽可能的多一些C++的语法，然后放低一下python的语法。比如在某一次任务中，还需要生成csv，我认为这个是没有必要去学的，还有画图等，另外同时学习python和C++经常会导致语法混淆，他们两个之间的语法还是差太多了，是否可以考虑先以C++为主，先学C++，后续再补python？作为搞无人机控制来说，C++的应用应当会比python多很多。希望更多地去训练工程架构思维和C++语法。比如C++这次的函数包装器实现回调函数这部分就很值得学习，并且通过lambda表达式来实现函数传递，将四个参数变成符合要求的两个参数，这种方式都很值得学习。另外我想尽可能地减少GTest和pytest，我体会不到其中的作用，反而徒增我的工作量。关于Gtest和pytest的使用我已经学会了，后续可以考虑不做这个测试了。
