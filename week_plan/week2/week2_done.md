# 第 02 周进度汇报

> 本文件从 2026-08-17 开始持续更新，周末再完成最终汇总。
>
> - `I`：Independent，独立完成；
> - `H`：Hint，在提示或讲解后自行完成；
> - `G`：Generated，由 Codex 直接生成，不计入个人掌握。

## 1. 总体完成度

- 自评完成度：待周末统计
- 周一至周六实际有效投入：待填写
- 是否使用周日突击补任务：待填写
- Windows 验收：进行中
- Ubuntu 辅助验收：未运行

## 2. 每日完成情况

### 周一

- 状态：完成
- 已完成：最小 C++ 子工程、限幅和向量有限性检查、演示程序、10 个 GoogleTest、GoogleTest/CTest 笔记、环境记录、两道 Hot 100 复习题
- 未完成：无
- 主要阻塞：无

### 周二

- 状态：完成（按实际结果验收）
- 已完成：任务 A Python 基础练习；任务 B NumPy 向量运算，包括加减、点积、叉积、模长、单位化、矩阵乘向量，以及零向量单位化抛出 `ValueError` 的失败策略；任务 C pytest，5 个测试通过，并完成一次故意失败、错误解读和修复；任务 D Matplotlib，绘制原始向量与旋转向量，补全坐标轴、图例、标题和单位，保存并查看 PNG，修改为 -45° 前完成方向预测；任务 E 已闭卷 AC 复习“字母异位词分组”，用户自评 A；新题“和为 K 的子数组”已完成并自评 D
- 未完成：无；字母异位词分组的计数 key 对比按用户决定从本次复习中排除
- 主要阻塞：无；“和为 K 的子数组”当前等级为 D，后续按复习规则巩固

### 周三

- 状态：完成（按实际结果验收）
- 已完成：任务 A 闭卷比较欧拉角、旋转矩阵和单位四元数的参数量、约束、优点与项目用途；任务 B 推导 ZYX 基本旋转矩阵与完整的 $R_{NB}=R_z(\psi)R_y(\theta)R_x(\phi)$，辨析主动旋转/坐标变换、固定轴/移动轴、列向量约定和万向节锁，并完成非零角度数值验证；任务 C 手算四元数共轭、一般逆、单位四元数逆、绕 z 轴 $90^\circ$ 的正向旋转与逆向恢复，明确 $q_{NB}$ 的 B 到 N 映射；任务 D 从空文件实现四元数共轭、Hamilton 乘法、归一化和向量旋转，6 个 pytest 全部通过；任务 E 已生成旋转矩阵/四元数三维姿态对比图并验证两者一致；任务 F “最长连续序列”自测 AC、评级 A，“盛最多水的容器”完成、评级 D，并解释双指针为何移动较短侧
- 未完成：无
- 主要阻塞：主动/被动、固定轴/移动轴和矩阵乘法顺序曾长期混淆，已通过几何推导和手机实验理清，仍需在周六闭卷复验；“盛最多水的容器”当前等级为 D，需后续闭卷重写

### 周四

- 状态：完成（按实际结果验收）
- 已完成：任务 A 理解状态方程 $\dot{x}=f(t,x)$，区分状态、状态变化率、动力学模型和积分器；任务 B 按 NED 向下为正的约定推导一维自由落体，得到 $\dot z=v_z$、$\dot v_z=g$、$z(t)=\frac12gt^2$ 和 $v_z(t)=gt$，并验证 $g=9.80665\ \mathrm{m/s^2}$ 时 $z(2)=19.6133\ \mathrm{m}$、$v_z(2)=19.6133\ \mathrm{m/s}$；任务 C 从空文件实现自由落体状态导数、显式 Euler 单步、非法步长检查、0—2 秒循环以及时间/状态记录；任务 D 完成 `dt=0.1、0.05、0.01` 的数值/解析对比和误差实验，生成位置、速度、位置误差及步长—终点误差图；任务 E 编写 6 个 pytest，周四测试 6/6、Week2 测试 17/17；任务 F 闭卷自测“无重复字符的最长子串”并 AC，用户自评 A
- 未完成：无
- 主要阻塞：起初把状态导数误解为需要从单个数值或差分 API 求得；曾把变化率随时间变化的位移误写成“最终变化率 × 全程时间”；首次使用 `range`、函数作为参数、返回值解包和 `pytest.raises` 时需要提示；已通过推导、报错定位和实验修正

### 周五

- 状态：完成（按用户确认的周五验收口径）
- 已完成：任务 A 已理解并确认可以闭卷写出 RK4，能解释起点/半步/终点导数采样、状态与状态导数的区别、标量与 Eigen 列向量的数乘、位置分量如何积分，以及自由落体为何适合作为基础积分器基准；任务 B 已加入 Python RK4、相同条件下的 Euler/RK4/解析解比较、4 个新增 pytest、误差对比图和具体结论，Week2 Python 测试通过 21/21；任务 C 已在 `cpp/exercises/Week2/Fri/` 建立 `Eigen::Vector2d` 状态、自由落体动力学、Euler、RK4、CMake target 和 20 步演示，配置、编译、链接和运行成功；任务 D 新增 7 个 GoogleTest，覆盖动力学与恒定重力、Euler/RK4 单步、两秒最终速度、位置误差比较、非法步长和有限值，GoogleTest/CTest 均通过 7/7；任务 E“找到字符串中所有字母异位词”已 AC，用户自评 D
- 未完成：无；用户明确决定不要求将 Python/C++ RK4 与 4 个完整生成的测试从 `G` 重写为 `I/H`
- 主要阻塞：无工程阻塞；Python RK4、C++ RK4 和部分测试仍保留 `G`，异位词题仍保留 D，均不改写为已独立掌握

### 周六

- 状态：进行中（任务 A 至 F 已完成；任务 D 有未通过项）
- 已完成：任务 A 已核对 Python/C++ 自由落体演示共同使用 `g=9.80665 m/s²`、初始状态 `[z, v_z]=[0, 0]`、`dt=0.1 s`、终止时间 `2 s`、20 个积分步和 NED 向下为正的坐标约定；本次已运行 Python 演示，得到 `dt=0.1` 时 Euler `[18.632635, 19.6133]`、RK4 `[19.6133, 19.6133]`，并复用周五已经记录的 C++ 演示结果完成跨语言核对；用户要求跳过本次手算误差步骤并将任务 A 标记为完成。任务 B 已确认新目录 `build/week2-sat-clean-20260822` 原先不存在，使用 Windows MinGW GCC Debug preset 完成 CMake 4.4.2 配置和全部目标编译，G++ 为 15.2.0，未出现项目源码编译警告或错误；配置阶段仅出现 Eigen 3.4.1 第三方源码的 CMP0146 OLD behavior 弃用警告；开始前回归复验为 Week2 Python 21/21、Week2 C++ 17/17，用户确认任务 B 完成，但本次未在新构建目录中另行运行 CTest。任务 C 为可选 Ubuntu 验证，用户按可选项口径确认完成，本次未新增 Ubuntu 构建结果。任务 D 已完成姿态闭卷验收：`R_NB/q_NB` 的 B 到 N 方向与向量公式为 `I`；`R_BN=R_NB^{-1}=R_NB^T` 及正交性解释为 `H`；一般四元数逆、单位四元数共轭与反向旋转为 `H`；欧拉角取舍与万向节锁为 `H`；旋转矩阵约束和取舍为 `H`；四元数取舍为 `I`；内禀 ZYX 与外禀 XYZ 的对应为 `H`；`q/-q` 双覆盖解释为 `H`；完整 ZYX 正余弦旋转矩阵由 Codex 直接给出，标记 `G`，不计为闭卷通过
- 任务 E 闭卷结果：GoogleTest 与 CTest 的区别在直接讲解后掌握，记录为 `H`；能够区分 GoogleTest 在 C++ 测试程序内部提供测试组织与断言、CTest 负责启动和汇总已注册测试、CMake/MinGW 构建测试可执行文件，并明确测试全部通过的进程退出码为 `0`。演示程序链接业务库、测试程序链接业务库与 `GTest::gtest_main` 的 target 关系在提示后答出，记录为 `H`。单项测试平时通过 VS Code 烧杯测试视图运行；GoogleTest `--gtest_filter=TestSuite.TestName` 命令行形式在提示后确认，按用户要求记录为 `H`。能够区分普通 Python 脚本、pytest 测试运行器和由脚本导入的 Matplotlib 绘图库；pytest 命令形式在提示后补全，平时通过 VS Code 测试视图运行，记录为 `H`。Euler 能说明每步在当前步起点计算一次状态导数以及减小步长的误差意义，完整通用公式由 Codex 补充后按用户要求记录为 `H`。RK4 的四次导数采样位置和 `1:2:2:1` 加权公式由 Codex 直接复述，用户选择按理解口径记录为 `H`，不作为本次闭卷独立写出。步长判断能够在讲解后说明使用同一积分器对 `dt`、`dt/2`、`dt/4` 做收敛实验并比较相同时刻关键状态，记录为 `H`
- 任务 F 结果：随机复测 49. 字母异位词分组、11. 盛最多水的容器、283. 移动零均已完成；周末评级依次为 `A`、`B`、`A`；11. 盛最多水的容器低于 A，进入第三周复习队列
- 未完成：任务 G
- 主要阻塞：无

## 3. I/H/G 统计

| 核心成果 | I/H/G | Codex 提供了什么帮助 | 能否从空文件重写 |
|---|---|---|---|
| 最小 CMake 子工程 | `H` | 解释 target、依赖传播、`PUBLIC`/`PRIVATE`/`INTERFACE`、Eigen 和 GoogleTest target，并检查和调整接入位置 | 待闭卷复验 |
| 限幅/向量检查 C++ | `H` | 提供接口、边界条件、Eigen API 和格式提示；核心实现由本人输入 | 待闭卷复验 |
| 演示程序和 GoogleTest | `H` | 提供测试覆盖思路、GoogleTest 宏和运行方法，并协助检查 | 待闭卷复验 |
| GoogleTest/CTest 笔记 | `H` | 讲解两者职责、测试发现和单项测试运行流程；笔记由本人整理 | 是，仍需后续闭卷回答验证 |
| 周一 README | `G` | Codex 直接补写和整理 | 否，不计入掌握 |
| Windows 环境记录 | `G` | Codex 查询实际版本并直接更新文档 | 否，不计入掌握 |
| 两数之和 | `I` | 未提供本次闭卷实现 | 是，本次已闭卷 AC |
| 移动零 | `I` | 未提供本次闭卷实现 | 是，本次已闭卷 AC |
| Python 基础和 NumPy | `H`（任务 A、B） | 逐步讲解 Python 语法、NumPy 数组、`*`/`@` 区别、向量运算和零向量失败策略；核心代码由本人从空文件输入并运行，Codex 仅直接执行了最终取消注释与恢复输出的机械整理 | 待闭卷复验 |
| pytest | `H` | 讲解测试发现、普通 `assert`、`assert_allclose`、`pytest.raises` 和单项测试命令；5 个测试由本人从空文件输入并运行 | 待闭卷复验 |
| pytest 失败记录 | `G` | Codex 根据本人运行得到的失败输出直接整理记录 | 否，不计入独立错误解读能力 |
| Matplotlib 绘图 | `H` | 逐步讲解 `Figure`/`Axes`、`quiver`、坐标设置、标注、保存和显示 API；脚本由本人从空文件输入、运行并修改角度验证 | 待闭卷复验 |
| 姿态表示关系与 ZYX 推导 | `H` | 通过逐步提问讲解基本旋转矩阵、基向量列、组合顺序、主动/被动、固定轴/移动轴和万向节锁；主要矩阵推导由本人逐列完成 | 待周六闭卷复验 |
| 姿态关系整理文档 | `G` | Codex 根据学习过程、疑问和推导直接整理《欧拉角和旋转矩阵的关系》 | 否，不计入掌握 |
| Python 四元数 | `H` | Codex 逐步解释数组 API、Hamilton 乘法、共轭、归一化、夹心乘法和测试思路；核心函数与测试由本人从空文件输入并修改 | 待闭卷复验 |
| 3D 姿态可视化 | `G` | Codex 直接生成旋转矩阵/四元数对比脚本和 PNG，并完成数值验证 | 否，不计入掌握 |
| Python Euler | `H` | 逐步讲解状态与导数、动力学模型/积分器分离、显式 Euler 公式、固定步数循环、浮点时间记录和异常策略；状态导数、Euler 单步与仿真循环由本人从空文件输入并修改 | 待闭卷复验 |
| 周四自由落体解析解与误差实验 | `H` | 讲解 NumPy 向量化、解析解、误差定义和一阶误差现象；解析函数与数值比较由本人输入和运行 | 待闭卷复验 |
| 周四自由落体绘图 | `G` | Codex 直接生成四子图绘图脚本和 PNG，并处理 Matplotlib 参数兼容问题 | 否，不计入掌握 |
| 周四 pytest | `H` | 逐步讲解测试命名、返回值解包、普通 `assert`、`assert_allclose`、`pytest.raises` 和有限值检查；6 个测试由本人输入并修正 | 待闭卷复验 |
| Python RK4 | `G` | Codex 直接实现 `rk4_step`、RK4 仿真、4 个 pytest、Euler/RK4/解析解比较脚本和结论文档 | 否，不计入掌握 |
| C++ Euler | `H` | Codex 逐步解释 Eigen 状态、动力学接口、步长检查和 Euler 公式；核心动力学、Euler 单步及循环由本人输入并修正 | 待闭卷复验 |
| C++ RK4 | `G` | 本人输入接口、步长检查和 `k1`；Codex 直接补写 `k2/k3/k4` 与最终加权更新 | 否，需从空文件重写 |
| 周五 CMake 与演示循环 | `H` | Codex 分步讲解 library/executable target、头文件路径、Eigen 依赖、C++17、链接关系和仿真循环；内容由本人逐步输入并完成构建运行 | 待闭卷复验 |
| 周五 GoogleTest | `H/G` 混合 | 动力学、Euler 单步和位置误差比较测试由本人在提示后完成；RK4 单步、两秒速度、非法 `dt` 和有限值测试由 Codex 提供完整代码 | 部分；`G` 测试不计入独立掌握 |
| RK4 公式、采样位置与自由落体基准解释 | `H` | Codex 讲解四个斜率、半步/完整步预测、向量数乘、位置分量积分和基准选择；用户确认可以闭卷验收 | 是（用户确认） |

## 4. 工程结果

- 干净构建：通过（Windows CMake Tools 清理构建产物与缓存后重新配置、构建和测试）
- 周一新增 C++ 测试：通过 10 / 总计 10
- 周五新增 C++ 测试：GoogleTest/CTest 通过 7 / 总计 7；Week2 周一与周五累计通过 17 / 总计 17
- Python 测试：Week2 周二至周五练习通过 21 / 总计 21；其中周四原有测试 6/6、周五 RK4 新增测试 4/4
- 新增 C++ 测试数：17（周一 10，周五 7）
- 新增 Python 测试数：21（周五新增 4）
- 项目源码新增警告：周一练习未发现；Eigen 上游 CMake 策略警告不计入项目源码警告
- 当前 Git commit：待周末填写
- Git 工作区是否干净：否，当前练习尚未提交

## 5. 闭卷验收

| 项目 | 通过/部分通过/未通过 | 主要问题 |
|---|---|---|
| 最小 CMake target | 通过 | 已完成并能解释主要语句，且已清理构建产物与缓存后重新配置和构建 |
| GoogleTest 与 CTest | 通过 | 已完成五问笔记并成功运行单项测试 |
| Python 运行与测试 | 通过 | 周二 3 个练习脚本语法检查通过；pytest 5/5，通过普通 `assert`、`assert_allclose` 和预期异常测试 |
| Matplotlib | 通过 | 已生成并查看 `vector_rotation.png`，包含原始/旋转向量、坐标轴、图例、标题和单位，并在修改为 -45° 前预测方向 |
| R_NB/q_NB | 部分通过 | 已完成 ZYX 推导并能说明 B 到 N 的方向；固定轴/移动轴与主动/被动的组合关系需周六再次闭卷确认 |
| 共轭与逆 | 部分通过 | 能计算一般四元数逆和单位四元数共轭，并完成 z 轴 $90^\circ$ 正逆恢复；需周六闭卷复验 |
| 欧拉角/矩阵/四元数 | 部分通过 | 能说明参数量、主要取舍和项目用途，四元数实现 6/6 测试通过；3D 可视化为 G，不计入本人掌握 |
| Euler | 通过（H） | 能区分动力学模型和积分器，手算并实现显式 Euler 单步，完成三种步长实验；仍需周六闭卷重写复验 |
| RK4 | 通过（用户确认） | 能解释公式、四个采样状态和自由落体基准；Python/C++ 实现中的 `G` 仍按原标记保留 |
| 自由落体推导 | 通过（H） | 能按 NED 向下为正推导速度和位置解析解，并解释速度准确而位置存在 Euler 误差的原因；仍需周六闭卷复验 |

## 6. 数值实验结果

### 周二：二维旋转的主动旋转与被动坐标变换

采用列向量约定，二维正向旋转矩阵为：

$$
R(\theta)=
\begin{bmatrix}
\cos\theta & -\sin\theta \\
\sin\theta & \cos\theta
\end{bmatrix}.
$$

主动旋转表示坐标系固定、物理向量旋转。若旋转前坐标为 $\mathbf{v}$，则旋转后坐标为：

$$
\mathbf{v}_{\mathrm{rotated}}=R(\theta)\mathbf{v}.
$$

被动坐标变换表示物理向量固定、坐标系旋转。若新坐标系相对旧坐标系旋转 $+\theta$，并且 $R(\theta)$ 的列是在旧坐标系中表示的新基向量，则新旧坐标满足：

$$
\mathbf{v}_{\mathrm{old}}=R(\theta)\mathbf{v}_{\mathrm{new}}.
$$

因此，从旧坐标求同一物理向量的新坐标时需要逆变换：

$$
\mathbf{v}_{\mathrm{new}}
=R(\theta)^{-1}\mathbf{v}_{\mathrm{old}}.
$$

旋转矩阵为正交矩阵，满足：

$$
R(\theta)^{-1}=R(\theta)^{\mathsf T}=R(-\theta),
$$

所以：

$$
\mathbf{v}_{\mathrm{new}}
=R(\theta)^{\mathsf T}\mathbf{v}_{\mathrm{old}}
=R(-\theta)\mathbf{v}_{\mathrm{old}}.
$$

以坐标系逆时针旋转 $90^\circ$ 为例，新基向量在旧坐标系中表示为：

$$
\mathbf{e}_{x'}=
\begin{bmatrix}0\\1\end{bmatrix},
\qquad
\mathbf{e}_{y'}=
\begin{bmatrix}-1\\0\end{bmatrix}.
$$

固定物理向量 $\mathbf{v}_{\mathrm{old}}=[1,0]^{\mathsf T}$ 可以写成：

$$
\mathbf{v}_{\mathrm{old}}
=0\,\mathbf{e}_{x'}-1\,\mathbf{e}_{y'},
$$

因此它在新坐标系中的坐标为：

$$
\mathbf{v}_{\mathrm{new}}=
\begin{bmatrix}0\\-1\end{bmatrix}.
$$

结论：坐标系向 $+\theta$ 旋转时，同一固定物理向量的坐标需要按 $-\theta$ 变换。工程代码中必须明确旋转矩阵从哪个坐标系映射到哪个坐标系。

### 周三：同一 ZYX 姿态的旋转矩阵与四元数验证

- 欧拉角：roll = $30^\circ$、pitch = $20^\circ$、yaw = $45^\circ$；
- 约定：$R_{NB}=R_z(\psi)R_y(\theta)R_x(\phi)$，$q_{NB}=q_z\otimes q_y\otimes q_x$；
- 两种方法分别旋转三个机体基轴；
- 两组结果的最大绝对差：$2.220\times10^{-16}$；
- 结论：在统一映射方向、旋转顺序和列向量约定后，旋转矩阵与单位四元数表示同一个姿态；差异仅为浮点舍入误差；
- 结果图：`python/exercises/Week2/Wed/zyx_attitude_comparison.png`。

- g：$9.80665\ \mathrm{m/s^2}$
- 终止时间：$2.0\ \mathrm{s}$
- 使用的 dt：$0.1\ \mathrm{s}$、$0.05\ \mathrm{s}$、$0.01\ \mathrm{s}$
- 解析最终位置/速度：$z(2)=19.6133\ \mathrm{m}$，$v_z(2)=19.6133\ \mathrm{m/s}$
- Euler 最终位置/速度与误差：`dt=0.1` 时 $z=18.632635\ \mathrm{m}$、$e_z=-0.980665\ \mathrm{m}$；`dt=0.05` 时 $z=19.1229675\ \mathrm{m}$、$e_z=-0.4903325\ \mathrm{m}$；`dt=0.01` 时 $z=19.5152335\ \mathrm{m}$、$e_z=-0.0980665\ \mathrm{m}$；三种步长的最终速度均为 $19.6133\ \mathrm{m/s}$，速度误差为 $0\ \mathrm{m/s}$
- RK4 最终位置/速度与误差：三种步长的最终位置和速度均为 $19.6133$；`dt=0.1` 时位置误差为 $0$，`dt=0.05` 时约为 $7.1054\times10^{-15}\ \mathrm{m}$，`dt=0.01` 时约为 $1.4211\times10^{-14}\ \mathrm{m}$，均为双精度浮点舍入量级；最终速度误差为 $0$ 或舍入量级
- 最重要的实验结论：常加速度下，Euler 对速度的每步增量 $g\,dt$ 可精确累计；位置更新使用区间起点速度，因此低估位移。步长减半时最终位置误差约减半，体现显式 Euler 的一阶全局误差；减小 `dt` 会缩短“变化率在单步内保持不变”的近似区间

## 7. Hot 100

| 题目 | 是否 AC | 周末等级 | 核心不变量 | 主要问题 |
|---|---|---|---|---|
| 两数之和 | 是 | 待填写 | 使用哈希表保存已遍历值；查询补数后再记录当前值 | 待填写 |
| 移动零 | 是 | 待填写 | 写指针之前始终是已经按原顺序放好的非零元素 | 待填写 |
| 字母异位词分组 | 是 | A（用户自评） | 对每个字符串排序，字母异位词得到相同的规范化 key，并按 key 分组 | 本次不采用计数 key；计划要求的排序 key/计数 key 对比未完成 |
| 最长连续序列 | 是 | A（用户自评） | 先放入哈希集合，只从不存在前驱 `num-1` 的元素开始向后延伸，避免重复扫描同一序列 | 本次自测闭卷 AC；C++ 使用 `unordered_set::count` 判断前驱是否存在 |
| 无重复字符的最长子串 | 是 | A（用户自评） | 在每次统计长度前，滑动窗口内所有字符互不重复 | 本次闭卷自测完成并 AC，无主要阻塞 |
| 和为 K 的子数组 | 是 | D | 当前前缀和为 $P_r$ 时，统计此前出现过多少个 $P_l=P_r-k$ | 看懂后仍不能独立实现；需巩固先查询后更新以及初始映射 $0\to1$ 的作用 |
| 盛最多水的容器 | 是 | D | 当前面积由较短边限制；保留短边而移动长边只会缩短宽度且不能提高水位上限，因此只能淘汰较短边 | 看过核心思路后仍不能独立实现；需后续闭卷重写双指针证明与代码 |
| 找到字符串中所有字母异位词 | 是 | D | 维护固定长度窗口与目标字符串的字符计数；窗口满足条件时记录左端点 | 已 AC，但当前评级为 D，尚不能独立稳定实现，需后续闭卷重写 |

随机复测的 3 道题：

1. 49. 字母异位词分组：已完成，周末评级 `A`
2. 11. 盛最多水的容器：已完成，周末评级 `B`，进入第三周复习
3. 283. 移动零：已完成，周末评级 `A`

第三周 Hot 100 复习队列：560. 和为 K 的子数组、11. 盛最多水的容器、438. 找到字符串中所有字母异位词。

## 8. 本周最有价值的三个产出

1.
2.
3.

## 9. 最大的三个阻塞

1.
2.
3.

## 10. 希望第三周如何调整

-
