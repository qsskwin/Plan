# 第 05 周进度汇报

> 本文件从 2026-09-14 开始持续更新。原计划日期仍保留为任务索引，实际执行按学习顺序滚动记录，不因日期顺延压缩任务。
>
> - `I`：Independent，独立完成；
> - `H`：Hint，在提示或讲解后自行完成；
> - `G`：Generated，由 Codex 直接生成或整理，不计入个人独立掌握。

## 1. 总体完成度

- 当前状态：进行中；周一、周二、周三、周四及周五任务 A、B、C 已经按当前约定完成，当前接续周五可选控制周期实验。
- 实际执行区间：2026-09-14 至今。
- 已完成：周一任务 A、B、C；周二任务 A、B、C；周三任务 A、B、C；周四任务 A、B、C、C++ 结果容器；周五任务 A、B、C；算法 21、141、160、234 按当前约定结束。
- 当前接续：周五可选控制周期实验；积分步长保持 `0.01 s`，控制周期从 `0.02 s` 改为 `0.1 s`。
- 尚未开始：周五可选控制周期实验，周六任务，以及算法 15、438。
- 主动跳过：算法 21 的代码、实际用时和循环不变量正式验收；算法 141、160 的代码、用时、正确性解释和边界用例复核；算法 234 的代码、AC 状态、实际用时、找中点/反转/比较解释及是否恢复原链表均未核对。四题只保留本人报告或自评，不改写为 Codex 正式核对通过。
- 实际有效投入与健康情况：尚未统计，不补写估计值。
- 当前验证范围：完成高度控制器定向测试、默认高度阶跃、`kd=2` 欠阻尼对照、初始高度误差下 PD 与固定 `T=mg` 的定向运行、结果容器接入后的默认阶跃定向运行、实际质量增加 10% 的稳态偏差定向运行、正负大误差下的推力上下限探测，以及工程整理后的演示定向构建、运行和 500 样本 CSV 导出；没有运行 C++ 全量回归、CTest 全量或 Python 测试。

## 2. 每日完成情况

### 周一

- 状态：完成
- 已完成：任务 A 反馈、平衡与恢复关系；任务 B 回调和 lambda 适配；任务 C 简谐振子 RK4 收敛实验。
- RK4 结果：两个相邻步长误差比分别为 `15.995208498949642` 和 `15.998808554293307`，均位于计划要求的 `15—17` 范围内；三组原始误差未在当前记录中提供，因此不补写具体数值。
- 算法 21：本人报告已 AC、自评 `A`；代码、实际用时和循环不变量正式验收按本人决定跳过。
- 证据边界：任务 B 已按本人最新说明完成，但本轮没有重新检查代码或补分 `I/H` 等级；不据此新增独立掌握结论。

### 周二

- 状态：完成
- 已完成：任务 A 高度 PD 控制方向、控制律和三组默认参数计算；任务 B 正式 C++ 高度 PD 控制器；任务 C 七项代表性行为检查。
- 控制器实现：`AltitudePdParameters` 保存名义质量、`kp`、`kd` 和推力上下限；`AltitudeCommand` 保存限幅前、后的推力；构造函数检查参数范围和有限性；`compute(...) const` 检查输入、计算控制量并限制实际推力。
- 修正过的问题：补充非有限参数检查；补齐右花括号；将错误的 `+kd*v_h` 改为制动所需的负号；将限幅对象从加速度命令改为单位为 N 的原始推力。
- 定向测试：`AltitudePdControllerTest.*` 共 7 项，结果为 7/7 通过。
- 证据边界：控制方向和物理作用为 `H`；完整公式、量纲展开和三组数值计算为 `G`；头文件声明框架为 `G`；构造函数和 `compute` 核心为 `H`；测试代码、断言和 CMake 测试接入为 `G`。

### 周三

- 状态：完成
- 已完成：任务 A 最小闭环调度；任务 B 默认 `0→1 m` 高度阶跃；任务 C C++ 与 Eigen 小专项；算法 141、160 按当前约定收口。
- 调度结果：积分步长 `0.01 s`，控制周期 `0.02 s`，仿真时长 `10 s`；共 1000 次积分、500 次控制更新。
- 调度约定：控制器在 `step=0,2,...,998` 更新；其他积分步保持上一次推力；终点 `t=10 s` 只记录；RK4 四个阶段读取同一份 `heldInput`；控制器不在导数回调内更新。
- 默认阶跃结果：末高度 `0.999999508304041 m`，末向上速度 `8.67823606339836e-07 m/s`；North/East 末位置和末速度均为 `0`。
- 本人解释：在 `t=2 s` 时，飞行器仍低于目标但已有较大向上速度，P 项继续拉近高度，D 项提前制动，使推力低于平衡推力并产生与向上速度相反的加速度；该解释记为 `H`。
- C++ 与 Eigen：本人在提问和局部纠正后完成状态顺序、索引、动态向量初始化、`head`/`tail`、`const StateVector&`、按值返回、lambda 保存及引用捕获生命周期的解释，整体记为 `H`。
- 算法 141、160：本人报告均已 AC、自评 `A`；本轮不再检查代码、用时、正确性解释或边界用例。
- 证据边界：演示文件框架、头文件、对象声明、lambda、CMake 接入和控制更新块为 `G`；本人在显式 TODO 框架下完成 `rk4Step` 调用和积分计数，记为 `H`；选择性观测输出为 `G`。

### 周四

- 状态：任务 A、B、C、C++ 结果容器及算法 234 已按当前约定完成
- 已完成：在质量匹配、推力未饱和、理想连续反馈和固定参考条件下，从 `e=h_ref-h` 推导出 `e_ddot+kd*e_dot+kp*e=0`；能够说明二阶来自误差的二阶导数，而不是平方。
- 特征方程：能够由 `e(t)=exp(s*t)` 写出一、二阶导数，认识 `s^2+kd*s+kp=0`；默认参数下得到重根 `s1=s2=-2`，并说明负实部使误差随时间衰减。
- 默认参数：正确计算 `omega_n=2 rad/s`、`zeta=1`，判断为临界阻尼；能区分欠阻尼可能振荡和超调、临界阻尼为无振荡条件下最快收敛、过阻尼无振荡但通常更慢，并能说明正实部会使误差增长而不收敛。
- 作答证据：固定参考下的一、二阶误差导数符号，指数函数求导，默认重根、负实部含义，自然频率、阻尼比和三类阻尼的最终回答均正确；初始条件最初误答为 `kp`、`kd`，经讲解后改正为 `e(0)`、`e_dot(0)`。
- 来源边界：完整公式来源、受力到控制律的逐步说明以及《高度PD误差方程推导.md》由 Codex 提供，记为 `G`；本人在这些材料和分步提问基础上完成推导、计算和物理解释，任务 A 整体记为 `H`，其中上述无提示回答作为局部 `I` 证据保留。
- 任务 B 预测：将 `kd` 从 4 改为 2 后，正确计算 `omega_n=2 rad/s`、`zeta=0.5` 并判断为欠阻尼；预测制动减弱、上升更快、出现超调。最初误认为参考切换瞬间的误差会变为 `1` 以外的数值，经纠正后认识到此时两组状态相同且 D 项均为零，所以初始推力同为 `20.709975 N`。
- 任务 B 手算：在 `e=0.5 m、v_h=0.5 m/s` 时，正确算出 `kd=4` 对应 `a_cmd=0 m/s²`、`kd=2` 对应 `a_cmd=1 m/s²`，后者推力大 `1.5 N`。
- 任务 B 运行：本人将 `kd` 改为 2 并运行同一阶跃；`t=2 s` 时高度 `0.8615074768223 m`、向上速度 `0.841049507168713 m/s`，均高于 `kd=4` 对照的 `0.601233194500802 m` 和 `0.537538165462029 m/s`；终点高度 `1.00012961462381 m`、向上速度 `-2.61731157914408e-05 m/s`，能够观察到超过目标后向下恢复的状态。
- 任务 B 结论：降低 `kd` 没有改变参考切换瞬间的初始推力，但运动开始后速度制动减弱，使上升更快并出现超调；最终仍趋近目标。当前选择性输出没有统计全程最大高度或最大速度，因此不补写精确峰值。
- 任务 B 来源边界：参数修改和程序运行由本人完成；阻尼、初始推力及响应变化在提问和一次纠正后完成解释，整体记为 `H`。
- 任务 C 预测：在 `h_ref=1 m、h(0)=0.5 m、v_h(0)=0` 下，正确得到初始误差 `0.5 m`，并判断固定 `T=mg` 时加速度为零、高度保持 `0.5 m`；初次将 PD 初始加速度写成 `-2 m/s²`，经符号纠正后得到 `a_cmd=2 m/s²`。初始 PD 推力 `17.709975 N` 及其比平衡推力多 `3 N` 的算术由 Codex 直接给出。
- 任务 C 运行：本人分别运行默认 `kp=4、kd=4` 的 PD 工况和将 `kp、kd` 均置零的固定平衡推力工况。PD 终点高度为 `0.999999957942584 m`、末速度为 `7.4270582198113e-08 m/s`；固定 `T=mg` 工况终点仍为 `0.5 m`、末速度为 `0 m/s`。
- 任务 C 结论：固定平衡推力只能令合力和加速度为零，不能感知或消除已有的 `0.5 m` 高度误差；PD 通过位置反馈产生恢复作用，并在出现向上速度后由 D 项减小推力、抑制速度增长。本人能够解释即使控制计算发生 500 次，`kp=kd=0` 时每次仍只得到相同的 `mg`，因此没有恢复作用。
- 任务 C 来源边界：工况修改和两次程序运行由本人完成；预测、符号纠正、数值计算和分步解释包含提示，整体记为 `H`，其中初始推力与推力差的算术记为 `G`。
- 结果容器：本人在 `altitude_pd_demo.cpp` 中定义含时间、参考高度、高度、向上速度、限幅前推力和实际推力的 `Sample`，使用 `std::vector<Sample>` 保存每次控制更新的数据，并提前为 500 个样本预留容量。
- C++ 理解：能够说明 `size` 是当前有效元素数、`capacity` 是已分配存储能够容纳的元素数；经纠正后区分 `reserve` 只预留容量、`resize` 会改变元素数量，并能解释扩容搬迁元素后旧引用、指针和迭代器可能失效。
- 容器运行：恢复默认阶跃后得到 1000 次积分、500 次控制更新、`sample count=500`、`sample capacity=500`；样本数与控制更新次数一致，预留容量覆盖了本次全部追加。
- 结果容器来源边界：结构体、容器声明和预留由本人在分步提示后完成；字段映射由本人正确回答，Codex 随后给出完整 `push_back` 和计数输出写法。概念与整体接入记为 `H`，直接给出的代码片段保留为局部 `G`。
- 算法 234：本人自评 `A`，按本人最新要求收口；本轮没有查看代码或 AC 记录，也没有验收找中点、反转后半段、逐项比较和恢复原链表的解释，因此只记录自评，不新增正式掌握证据。
- 算法 21：本人再次确认自评 `A`；沿用此前“本人报告已 AC、自评 A”的记录，未产生新的代码、用时、循环不变量或正确性解释证据。
- 运行边界：任务 A 没有运行程序；任务 B、C 和结果容器只运行 `altitude_pd_demo` 定向工况，没有运行全量回归。
- 后续已进入周五任务，结果见下一节。

### 周五

- 状态：任务 A、B、C 已完成；可选控制周期实验及算法 15、438 尚未开始。
- 任务 A 工况：控制器名义质量保持 `1.5 kg`，实际质量改为 `1.65 kg`；`h_ref=1 m`、`h(0)=1 m`、`v_h(0)=0`，`kp=4 s^-2`、`kd=4 s^-1`，仿真 10 秒。
- 预测与纠正：本人正确判断初始误差为零、初始推力小于实际重力、飞行器先向下运动，以及纯 PD 无法消除质量偏差造成的稳态误差；最初把 `a_cmd=0` 与控制器输出推力混为一谈，并误判下降后的高度符号和 D 项方向，经纠正后确认下降时 `h` 仍可为正、`e>0`、`v_h<0`，P 项与 D 项都会增大推力。
- 理论计算：Codex 按本人给出的平衡思路完成算术，得到实际重量和稳态推力 `16.1809725 N`、理论稳态误差 `0.24516625 m`、理论稳态高度 `0.75483375 m`；数值手算记为 `G`。
- 工况修改：本人根据明确的目标参数将实际质量、固定参考和 NED 初始高度接入演示，记为 `H`；Codex 修正注释并直接完成最后一秒平均误差统计代码，具体实现记为 `G`。
- 统计思路：本人独立提出遍历 `samples`、筛选 `timeS>=9.0`、累计误差与计数并求平均，记为局部 `I`；经提示改用 `sample.referenceHeightM` 并在循环结束后统一除法，记为局部 `H`。
- 定向运行：构建并运行 `altitude_pd_demo` 成功；1000 次积分、500 次控制更新、500 个样本。最后一秒包含 50 个控制更新样本，平均高度误差为 `0.2451662673519 m`；终点高度为 `0.754833745725549 m`，终点向上速度为 `1.08622248512476e-08 m/s`。
- 验收结论：最后一秒平均误差与理论值只差约 `1.73519e-08 m`，小于计划阈值 `0.005 m`；末速度绝对值小于 `0.02 m/s`。结果支持“纯 PD 在常值质量偏差下可以稳定，但需要非零位置误差提供额外推力”的模型结论。
- 运行边界：本任务只定向构建并运行 `altitude_pd_demo`；没有运行全量回归，也没有统计全程推力极值或饱和比例。
- 任务 B 预测：本人正确判断正大误差会触发上限、负大误差会触发下限，动力学应使用 `appliedThrustN`，并说明同时保留 `rawThrustN` 与 `appliedThrustN` 是为了观察是否限幅；经补充后区分“原始命令可以为负”和“实际推力不能为负”，并确认零推力不等于向下推力，飞行器仍受重力向下加速。
- 任务 B 实现：本人根据提示增加两组独立 `compute` 调用和输出。第一次将探测放在控制更新循环内且没有输出；经指出后移到仿真循环外，使每组只计算一次并打印两个推力字段。探测命令未写入 `heldInput`，没有改变质量偏差轨迹；实现过程记为 `H`。
- 任务 B 运行：上限工况 `h_ref=10 m、h=0、v_h=0` 得到 `rawThrustN=74.709975 N`、`appliedThrustN=29.41995 N`；下限工况 `h_ref=0、h=10 m、v_h=0` 得到 `rawThrustN=-45.290025 N`、`appliedThrustN=0 N`，均与预测一致。
- 任务 B 结论：原始推力表示控制器希望施加的力，实际推力表示限幅后被控对象能够收到的力；本次只验证两个静态大误差输入，没有运行可选的 `0→10 m` 饱和轨迹，也没有统计现有轨迹的全程饱和比例。
- 任务 C 职责整理：Codex 将演示重构为 `AltitudePdController::compute` 负责计算控制、`runCase` 负责工况调度与采样、`summarizeCase`/`printSummary` 负责汇总和显示、`exportSamplesCsv` 负责读取样本并导出文件；重构代码整体记为 `G`。
- 任务 C 控制器边界：检查 `altitude_pd.hpp/.cpp` 后确认控制器只验证参数和输入、计算并限幅推力；内部没有绘图、文件操作、RK4 或动力学积分。静态限幅探测与主闭环输入保持隔离。
- 任务 C 文档：README 新增高度 PD 入口、定向构建/运行及可选 CSV 命令、四项职责表、调用关系和对象生命周期；`Qss.md` 更新为当前核心模块、应用、测试、Python 包、pytest 和文档现状，移除“只有早期骨架、尚无 pytest”等过时描述。两份文档修改均为 `G`。
- 任务 C 生命周期：`controller` 由 `main` 持有并覆盖同步仿真；`plantParameters`、`heldInput` 和状态由 `runCase` 持有；动力学 lambda 只在 `runCase` 返回前按引用读取局部对象；返回的 `CaseResult` 按值拥有终态和样本，后续汇总与导出只读。
- 任务 C 定向验证：`altitude_pd_demo` 构建和运行通过，质量偏差终点与任务 A 完全一致；使用 `--output-csv` 成功生成 501 行文件，其中 1 行表头、500 行控制更新样本，首个样本时间为 `0 s`、末个样本时间为 `9.98 s`。
- 任务 C 验证边界：没有运行 GTest、CTest 或 pytest 全量回归；CSV 位于忽略的构建目录，不作为源码成果提交。

### 周六

- 状态：未开始
- 计划内容：必要回归、关键行为验收、简短解释修改和周报收口。

## 3. I/H/G 统计

| 核心成果 | I/H/G | Codex 提供了什么帮助 | 当前能力或证据边界 |
|---|---|---|---|
| 反馈、平衡与恢复关系 | `H` | 通过具体状态和方向问题引导判断 | 已完成；不重新执行早期问答 |
| 回调与 lambda 适配 | 完成，未重新细分 | 讲解 `DerivativeFunction` 与四参数动力学函数的签名适配 | 本轮未复核代码，不新增独立等级 |
| RK4 收敛实验 | 完成，未重新细分 | 复用正式 `rk4Step`，按计划核对误差比 | 两个误差比满足 `15—17`；原始误差未记录 |
| 高度控制方向和物理作用 | `H` | 通过低于、高于目标及向上运动三种状态引导 | 能解释 P 项恢复、D 项制动 |
| 完整控制律、量纲与数值手算 | `G` | Codex 提供完整公式、量纲展开和三组数值计算 | 不计为独立手算能力 |
| 控制器头文件声明框架 | `G` | Codex 直接提供接口框架 | 不计为独立接口设计 |
| 控制器构造函数与 `compute` | `H` | 对有限性、花括号、D 项符号和限幅对象给予局部提示 | 本人完成并通过定向行为检查 |
| 高度控制器测试 | `G` | Codex 直接完成测试框架、断言、重复输入和 CMake 接入 | 7/7 通过不等于本人独立掌握 GoogleTest 编写 |
| 闭环调度与默认阶跃 | `H/G` 混合 | Codex 提供演示框架、lambda、控制更新块和输出；本人补入积分调用与计数 | 调度解释为 `H`，不把整段闭环循环登记为独立实现 |
| C++ 与 Eigen 小专项 | `H` | 通过提问和局部纠正完成概念检查 | 能解释状态索引、引用、返回值和捕获生命周期 |
| 二阶误差方程、特征根与阻尼 | `H`（含局部 `I`） | Codex 提供公式来源说明、推导笔记和分步纠错 | 能完成默认参数计算、阻尼分类和根实部判断；完整材料来源仍为 `G`，不改写为独立推导 |
| `kd=2` 欠阻尼响应对照 | `H` | 通过预测问题纠正参考切换瞬间的状态判断，并引导对照输出 | 本人修改参数、运行程序并正确解释制动减弱、上升加快、超调和最终恢复；精确峰值未统计 |
| 初始高度误差恢复对照 | `H`（含局部 `G`） | 纠正初始加速度符号，直接给出初始推力和推力差算术，引导两个工况 | 本人完成两次运行并能解释固定 `mg` 不能消除误差、PD 可以恢复；直接给出的算术不计为独立计算 |
| `std::vector<Sample>` 结果容器 | `H/G` 混合 | 分步检查位置和重复定义；直接给出完整 `push_back` 与计数输出写法 | 能解释 `size`、`capacity`、`reserve`、`resize` 和扩容导致引用失效；本人完成接入并定向运行，直接给出的代码不计为独立实现 |
| 质量增加 10% 的稳态偏差 | `H/G` 混合（含局部 `I`） | Codex 纠正初始输出、下降时高度和 D 项方向，完成数值手算、平均误差代码及定向运行 | 本人正确给出主要预测和非零稳态误差结论，并独立提出最后一秒平均误差统计算法；工况修改为 `H`，具体统计代码和算术为 `G` |
| 推力上下限探测 | `H`（含局部 `I`） | Codex 补充原始负推力与零实际推力的区别，指出探测不应位于控制循环内，并给出预期数值 | 本人独立判断限幅方向、动力学使用 `appliedThrustN` 及保留两个字段的作用；两次修改后完成单次探测和输出，具体数值由 Codex 预先给出 |
| 工程职责整理与文档 | `G` | Codex 直接重构 `runCase`、汇总、CSV 导出和命令行入口，并更新 README、Qss 与调用关系/生命周期说明 | 定向构建、运行及 CSV 结构已验证；不计为本人独立重构或文档设计能力 |
| 选择性观测输出 | `G` | Codex 直接补充指定时刻输出 | 不计为本人独立实现 |
| 周报整理 | `G` | Codex 根据实际证据统一结构和状态 | 不计入控制或 C++ 掌握 |

## 4. 工程结果

- 正式控制模块：`cpp/include/core/altitude_pd.hpp`、`cpp/src/altitude_pd.cpp` 已接入 `aerial_core`。
- 代表性测试：`cpp/tests/altitude_pd_test.cpp` 已接入 `aerial_core_gtest`。
- 闭环演示：`cpp/apps/altitude_pd_demo.cpp` 已作为 `altitude_pd_demo` 目标接入工程。
- 核心库定向构建：通过，命令为 `cmake --build .\build\windows-mingw-gcc-debug --target aerial_core -j 4`。
- GoogleTest 目标定向构建：通过，命令为 `cmake --build .\build\windows-mingw-gcc-debug --target aerial_core_gtest -j 4`。
- 高度控制器定向测试：`AltitudePdControllerTest.*` 通过 7/7。
- 演示定向构建与运行：默认阶跃、欠阻尼 `kd=2` 对照、初始高度误差下的 PD 与固定 `T=mg` 工况、结果容器接入后的默认阶跃、实际质量增加 10% 的稳态偏差工况，以及两组静态推力限幅探测均完成定向运行；动态工况均记录到 1000 次积分、500 次控制更新和 `10 s` 终点。
- 结果容器：当前质量偏差工况的 500 次控制更新保存为 500 个 `Sample`；最后一秒 `[9.0,10.0)` 共筛选出 50 个样本。
- 工程职责：`altitude_pd_demo` 已拆分工况运行、指标汇总、终端显示、静态限幅探测和可选 CSV 导出；核心控制器保持无 I/O、无绘图、无动力学积分。
- CSV 定向导出：`build/windows-mingw-gcc-debug/altitude_pd_mass_bias.csv` 共 501 行，表头字段和首末样本已检查；该文件位于忽略的构建目录。
- 文档入口：`README.md` 已增加高度 PD 运行与架构说明；`Qss.md` 已按当前工程内容更新。
- 未运行：C++ 全量回归、CTest 全量、Python 测试、周一任务重跑、全程推力极值与饱和比例统计。
- 构建目录：继续使用 `build/windows-mingw-gcc-debug`。旧 `build-ninja` 缓存仍指向项目改名前路径，不能直接重新生成，但未删除或清理。
- 构建警告：出现 Eigen 上游 CMake 策略弃用警告，不影响本次定向编译与运行结果。
- 当前 Git commit：`e4fbad5`（`20260910Thu5`）。
- Git 工作区：`altitude_pd_demo.cpp` 保留质量偏差工况和限幅探测并完成职责重构，`README.md`、`Qss.md` 与 `week5_done.md` 已更新；四个文件均为未暂存修改。《高度PD误差方程推导.md》已由当前提交跟踪。

## 5. 阶段验收

| 项目 | 通过/部分通过/未通过 | 主要问题或边界 |
|---|---|---|
| 反馈、平衡与恢复关系 | 通过（H） | 已完成，不重开旧问答 |
| 高度 PD 控制方向 | 通过（H） | 能解释低于目标增推力、向上速度产生制动 |
| 完整控制律、量纲与手算 | 部分通过 | 公式和数值由 Codex 提供，记为 `G` |
| C++ 高度控制器 | 通过（H/G） | 构造函数和核心计算为 `H`，声明框架为 `G` |
| 代表性行为检查 | 工程检查通过 | 7/7 通过；测试代码来源为 `G` |
| 采样保持与闭环调度 | 通过（H/G） | 能解释整数步调度；整体框架不能登记为独立实现 |
| 默认高度阶跃 | 通过 | 末误差和末速度满足计划中的 `0.02` 阈值 |
| C++ 与 Eigen 小专项 | 通过（H） | 概念回答在提问和局部纠正后完成 |
| 二阶误差方程与阻尼概念 | 通过（H，含局部 I） | 能完成默认推导、重根、收敛、自然频率、阻尼比和三类阻尼解释；完整推导笔记为 `G` |
| `kd=2` 欠阻尼响应对照 | 通过（H） | 预测和定向运行均完成；观察到上升更快、超调及向目标恢复，但未统计精确峰值 |
| 初始高度误差恢复 | 通过（H，含局部 G） | PD 恢复与固定 `T=mg` 不恢复的两组定向运行均完成；初始推力算术由 Codex 给出 |
| C++ 结果容器 | 通过（H/G） | 保存 500 个控制更新样本并完成定向运行；概念为 `H`，Codex 直接给出的代码片段为局部 `G` |
| 质量偏差实验 | 通过（H/G，含局部 I） | 最后一秒平均误差与理论值相差约 `1.73519e-08 m`，末速度满足阈值；统计代码和数值手算为 `G` |
| 推力上下限探测 | 通过（H，含局部 I） | 正误差原始推力超过上限后限制为 `29.41995 N`；负误差原始推力为负后限制为 `0 N`；探测未参与动力学 |
| 工程职责整理 | 工程检查通过（G） | 四项职责已分开，README/Qss 已更新；定向构建、运行和 CSV 导出通过，未做全量回归 |
| 采样周期实验 | 未通过 | 尚未运行，不能提前登记完成 |

## 6. 数值实验结果

### 周一：RK4 收敛实验

- 实验位置：`cpp/exercises/Week5/Mon/task_C`。
- 相邻步长误差比：`15.995208498949642`、`15.998808554293307`。
- 验收范围：两个比值均位于 `15—17`。
- 未记录：三组原始误差，因此不补写具体数值。

### 周二：默认参数下的三组控制器输入

默认参数：名义质量 `1.5 kg`，重力 `9.80665 m/s²`，`kp=4 s⁻²`，`kd=4 s⁻¹`，推力范围 `[0, 29.41995] N`。

| 输入 | 误差与加速度命令 | 限幅前 / 实际推力 | 物理含义 |
|---|---|---|---|
| `h_ref=1 m, h=1 m, v_h=0 m/s` | `e=0 m, a_cmd=0 m/s²` | `14.709975 N / 14.709975 N` | 重力前馈维持平衡 |
| `h_ref=1 m, h=0 m, v_h=0 m/s` | `e=1 m, a_cmd=4 m/s²` | `20.709975 N / 20.709975 N` | 低于目标，增加推力恢复 |
| `h_ref=1 m, h=1 m, v_h=0.5 m/s` | `e=0 m, a_cmd=-2 m/s²` | `11.709975 N / 11.709975 N` | 已到目标但仍向上，减小推力制动 |

以上完整计算由 Codex 提供，数值手算记为 `G`。

### 周三：默认高度阶跃

- 工况：初态六维状态全零；实际质量与名义质量均为 `1.5 kg`；固定单位姿态；`0≤t<1 s` 时参考高度为 `0 m`，从 `t=1 s` 起为 `1 m`。
- 调度：积分步长 `0.01 s`，控制周期 `0.02 s`，仿真时长 `10 s`；1000 次积分、500 次控制更新。
- `t=0.98 s`：参考 `0 m`，高度和向上速度均为 `0`，推力 `14.709975 N`。
- `t=1.00 s`：参考切换为 `1 m`，高度和向上速度仍为 `0`，推力升至 `20.709975 N`。
- `t=1.02 s`：高度 `0.0008 m`，向上速度 `0.08 m/s`，推力回落至 `20.225175 N`。
- `t=2.00 s`：高度 `0.601233194500802 m`，向上速度 `0.537538165462029 m/s`，推力 `13.877346840223 N`。
- `t=9.98 s`：高度 `0.999999490635865 m`，向上速度 `8.98993995169885e-07 m/s`，推力 `14.7099726622208 N`。
- 终点：高度 `0.999999508304041 m`，向上速度 `8.67823606339836e-07 m/s`；高度误差约 `4.91695959e-07 m`；North/East 末位置和末速度均为 `0`。
- 已观测样本中 `rawThrustN == appliedThrustN`；尚未统计全程推力极值或饱和比例，不能据此声称“全程未饱和”。

### 周四任务 A：二阶误差方程

- 使用假设：实际质量与名义质量匹配、推力未饱和、理想连续反馈、固定高度参考、当前高度动力学和 PD 控制律不变。
- 误差关系：固定参考下 `e_dot=-h_dot`、`e_ddot=-h_ddot`。
- 物理与控制关系：由竖直方向牛顿第二定律得到 `h_ddot=T/m-g`；控制器通过重力前馈和 PD 加速度命令给出 `T=m*(g+kp*e-kd*h_dot)`。
- 合并结果：`e_ddot+kd*e_dot+kp*e=0`。
- 特征方程：`s^2+kd*s+kp=0`；默认参数得到 `(s+2)^2=0` 和 `s1=s2=-2`。
- 默认参数解释：`omega_n=sqrt(kp)=2 rad/s`，`zeta=kd/(2*sqrt(kp))=1`，属于临界阻尼。
- 概念结论：负实部对应误差衰减；欠阻尼可能振荡和超调；临界阻尼是在无振荡条件下最快收敛；过阻尼通常无振荡但收敛更慢。
- 本任务没有运行代码；没有产生新的构建、测试或仿真结果。

### 周四任务 B：`kd=2` 欠阻尼对照

- 仅将 `kd` 从 4 改为 2；`kp=4`、质量、推力限制、积分步长、控制周期、参考阶跃和仿真时长保持不变。
- 连续模型参数：`omega_n=2 rad/s`、`zeta=0.5`，属于欠阻尼；这用于解释采样闭环结果，不要求连续解与采样结果逐点相同。
- 参考切换瞬间：两组均为 `e=1 m、v_h=0 m/s`，所以 D 项为零，初始推力均为 `20.709975 N`。
- `t=1.02 s`：高度 `0.0008 m`、向上速度 `0.08 m/s`、推力 `20.465175 N`；相同状态下因 `kd` 更小，推力高于默认对照的 `20.225175 N`。
- `t=2.00 s`：高度 `0.8615074768223 m`、向上速度 `0.841049507168713 m/s`、推力 `13.0177816175601 N`；默认 `kd=4` 对照分别为 `0.601233194500802 m`、`0.537538165462029 m/s` 和 `13.877346840223 N`。
- `t=9.98 s`：高度 `1.0001300406244 m`、向上速度 `-1.6426943582854e-05 m/s`，说明此时略高于目标且正在向下恢复。
- 终点：高度 `1.00012961462381 m`、向上速度 `-2.61731157914408e-05 m/s`；North/East 末位置和末速度均为 `0`。
- 计数：1000 次积分、500 次控制更新，与默认对照一致。
- 结论边界：已观测到超调和更快的上升过程，但当前程序只输出少量时刻，没有计算全程最大高度、最大速度、推力极值或饱和比例。

### 周四任务 C：初始高度误差恢复

- 共同工况：`h_ref=1 m`、`h(0)=0.5 m`、`v_h(0)=0`，质量 `1.5 kg`，积分步长 `0.01 s`，控制周期 `0.02 s`，仿真时长 `10 s`。
- PD 工况：`kp=4 s^-2`、`kd=4 s^-1`。`t=0.98 s` 时高度 `0.795185602825731 m`、向上速度 `0.274330359735949 m/s`；`t=2 s` 时高度 `0.955166190935376 m`、向上速度 `0.0713234738076922 m/s`。
- PD 终点：高度 `0.999999957942584 m`、向上速度 `7.4270582198113e-08 m/s`，初始 `0.5 m` 误差基本被消除。
- 固定平衡推力工况：将 `kp`、`kd` 均设为零，使每次控制计算都得到 `T=mg=14.709975 N`。所有已输出时刻的高度均为 `0.5 m`、速度均为 `0 m/s`。
- 固定平衡推力终点：高度 `0.5 m`、速度 `0 m/s`，最终高度误差仍为 `0.5 m`。
- 两组计数：均为 1000 次积分、500 次控制更新。控制更新次数只表示调度次数；当反馈增益为零时，重复计算 500 次仍不会产生恢复作用。
- 对照结论：平衡推力负责抵消重力，不能单独修正位置误差；PD 的 P 项根据位置误差产生恢复加速度，D 项根据速度产生制动，使系统回到目标并停下。

### 周四 C++ 专项：结果容器

- `Sample` 字段：`timeS`、`referenceHeightM`、`heightM`、`verticalSpeedMps`、`rawThrustN`、`appliedThrustN`。
- 容器策略：创建空的 `std::vector<Sample>`，调用 `reserve(kNumIntegrationSteps / kControlStride)` 预留至少 500 个元素的容量，再在每次控制更新时 `push_back` 一个真实样本。
- 记录语义：高度和速度使用已经完成 NED 符号转换、且实际传入控制器的 `heightM` 与 `verticalSpeedMps`，避免直接保存 `state(2)`、`state(5)` 造成字段语义或符号错误。
- 运行结果：`integration count=1000`、`control update count=500`、`sample count=500`、`sample capacity=500`；末高度和末速度与此前默认阶跃一致。
- 容量结论：本次实现的预留容量恰好为 500；C++ 只保证 `reserve(500)` 后容量不小于 500。预留完成后追加 500 个样本不需要再次因容量不足而扩容。
- 引用边界：若追加时容量不足，vector 可能申请更大的连续内存并移动或复制元素，指向旧元素的引用、指针和迭代器可能失效；本次没有编写故意使用失效引用的演示代码。

### 周五任务 A：实际质量增加 10%

- 工况：控制器名义质量 `1.5 kg`，被控对象实际质量 `1.65 kg`；固定 `h_ref=1 m`，初始 `h=1 m`、`v_h=0`，其余增益、推力限制和调度参数不变。
- 初始关系：控制器名义前馈推力为 `14.709975 N`，小于实际重量 `16.1809725 N`，因此飞行器最初向下运动。
- 理论稳态：由 `m_nominal*(g+kp*e_inf)=m_actual*g` 得到 `e_inf=0.24516625 m`、`h_inf=0.75483375 m`、`T_inf=16.1809725 N`；稳态速度为零，所以 D 项为零。
- 过程样本：`t=0.98 s` 时高度 `0.857613257036636 m`、向上速度 `-0.138849225526496 m/s`、推力 `16.3973908109392 N`；下降时正位置误差和负向上速度使 P、D 两项都增加推力。
- 终点结果：高度 `0.754833745725549 m`，向上速度 `1.08622248512476e-08 m/s`；终点高度误差为 `0.245166254274451 m`。
- 最后一秒统计：`[9.0,10.0)` 共 50 个控制更新样本，平均高度误差 `0.2451662673519 m`，与理论稳态误差相差约 `1.73519e-08 m`。
- 调度与容器：1000 次积分、500 次控制更新、`sample count=500`、`sample capacity=500`；North/East 末位置和末速度均为零。
- 结论：纯 PD 能使质量偏差工况稳定，但名义重力前馈不足，必须保留正的位置误差才能由 P 项补足推力，因此不能精确回到 `1 m`。
- 验证边界：只完成 `altitude_pd_demo` 的定向构建和运行；没有全量回归，也没有统计全程推力极值或饱和比例。

### 周五任务 B：推力上下限

- 参数：控制器名义质量 `1.5 kg`，`kp=4 s^-2`、`kd=4 s^-1`，推力范围 `[0,29.41995] N`；两组探测速度均为零。
- 上限工况：`h_ref=10 m、h=0 m`，原始推力 `74.709975 N`，实际推力被限制为 `29.41995 N`。
- 下限工况：`h_ref=0 m、h=10 m`，原始推力 `-45.290025 N`，实际推力被限制为 `0 N`。
- 数据流：探测命令只用于输出，没有写入 `heldInput`；质量偏差仿真的动力学仍只接收主控制命令的 `appliedThrustN`。
- 物理解释：负原始推力表示当前控制律希望继续减小向上推力，不表示模型获得向下推力；限幅到零后仍有重力，因此飞行器会向下加速。
- 证据边界：验证了两个静态输入下的上下限、原始命令和实际命令差异；没有运行可选的 `0→10 m` 饱和轨迹，也没有统计全程饱和比例。

### 周五任务 C：工程整理

- 职责划分：控制计算位于核心库 `AltitudePdController::compute`；演示中的 `runCase` 只负责创建工况局部对象、控制采样、命令保持、RK4 推进和保存样本；`summarizeCase` 计算最后一秒平均误差等指标；`exportSamplesCsv` 只读取样本并写 CSV。
- 调用关系：`main -> runCase -> controller.compute / rk4Step -> dynamics lambda -> pointMassDerivative`；运行结束后依次调用汇总、打印、静态限幅探测，并在显式给出 `--output-csv` 时导出数据。
- 生命周期：`main` 拥有控制器和工况定义；`runCase` 局部拥有动力学参数、保持输入和状态，lambda 的引用只在这些对象存活期间同步使用；`CaseResult` 按值返回并拥有终态和 `samples`。
- 控制器审查：`altitude_pd.hpp/.cpp` 中没有绘图、文件操作或动力学积分；输出仍包含限幅前、后的推力。
- README/Qss：新增高度控制入口、运行命令、CSV 用法、职责表及生命周期图；同时把项目结构、C++/Python 模块和 pytest 状态更新为当前事实。
- 定向验证：重构后终点高度 `0.754833745725549 m`、末速度 `1.08622248512476e-08 m/s`、最后一秒平均误差 `0.2451662673519 m`，与任务 A 结果一致；静态限幅结果也与任务 B 一致。
- CSV 验证：显式输出文件共有 501 行，表头为时间、参考高度、高度、向上速度、限幅前推力和实际推力，包含 500 个控制更新样本。
- 来源边界：本任务的重构、CSV 实现、调用关系图和文档均由 Codex 直接完成，统一记为 `G`；通过定向运行只证明工程结果可用，不升级为本人独立掌握。

### 尚未运行的实验

- 控制周期从 `0.02 s` 改为 `0.1 s` 的对照实验。
- 全程推力极值与按控制更新次数统计的饱和比例。

## 7. Hot 100

| 题目 | 本周状态 | 当前等级或自评 | 验收边界 |
|---|---|---|---|
| 21. 合并两个有序链表 | 本人报告已 AC | A（本人再次确认） | 代码、用时和循环不变量正式验收已跳过；本次无新增正式证据 |
| 206. 反转链表 | 本周未复习 | 沿用第四周 `B` | 无第五周新证据 |
| 141. 环形链表 | 本人报告已 AC | A（本人自评） | 未检查代码、用时、相遇解释或边界用例 |
| 160. 相交链表 | 本人报告已 AC | A（本人自评） | 未检查代码、用时、等长路程解释或边界用例 |
| 234. 回文链表 | 按本人要求收口 | A（本人自评） | 未查看代码或 AC 记录；未验收中点、反转、比较及是否恢复原链表，不记为正式核对通过 |
| 15. 三数之和 | 未开始 | 沿用第四周 `B` | 无第五周新证据 |
| 438. 找到字符串中所有字母异位词 | 未开始 | 沿用第四周 `B` | 无第五周新证据 |

## 8. 本周最有价值的三个产出

1. 完成正式 C++ 高度 PD 控制器及七项定向行为检查，明确重力前馈、位置恢复、速度制动、有限性检查和推力限幅的职责。
2. 将控制器接入现有三维平动模型与 RK4，跑通采样保持闭环，并用 `std::vector<Sample>` 保存 500 个控制更新样本；演示已进一步拆分工况、汇总和 CSV 导出职责，但该重构来源为 `G`。
3. 从误差定义、竖直受力和 PD 控制律连接到二阶误差方程，并通过 `kd=4→2`、“PD 对比固定 `mg`”和质量增加 10% 三组实验，把阻尼、速度制动、初始误差恢复及“稳定不等于零稳态误差”联系起来。

## 9. 最大的三个阻塞

1. 控制器实现中曾出现 D 项符号、限幅对象和非有限参数检查错误；这些问题已经修正，但说明符号和单位仍需要在新推导中继续主动检查。
2. 闭环演示的框架、lambda、控制更新块、观测输出及任务 C 的职责重构仍有较多 `G` 来源；本人已经完成局部实现和解释，但不能把整段闭环循环或本次重构登记为独立实现。
3. 当前只有定向测试和定向演示证据；全量回归、控制周期变化、精确响应峰值及全程饱和统计尚未执行，不能提前扩大结论。

## 10. 接下来如何调整

- 周四任务 A、B、C、C++ 结果容器、周五任务 A、B、C 及算法 234 已按当前约定完成；算法 21、234 的最新自评均为 `A`。
- 下一步按计划是周五可选控制周期实验：积分步长保持 `0.01 s`，只把控制周期从 `0.02 s` 改为 `0.1 s`；该项属于余量实验，尚未开始。
- 如需运行检查，只运行当前任务真正需要的定向范围；不重跑周一至周三任务，也不无故运行全量回归。
- 保留真实的 `I/H/G`、实际运行证据、跳过项和未验证边界；不因后续理解提高而改写早期 `G` 来源。
