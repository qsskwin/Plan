# 第 06 周进度汇报

## 1. 执行情况

- 原计划周期：2026-09-14 至 2026-09-20
- 实际执行区间：2026-09-20 起
- 预排 28 小时 / 实际有效投入：待周末汇总
- 健康与恢复情况：待周末汇总
- 已完成：周一任务 A“区分被控对象、控制器、闭环系统、参数与状态”；周一任务 B“规划新模块的数据流”；周一 C++ 专项“声明、定义与参数传递”；周二任务 A“从小旋转理解四元数导数”；周二任务 B“三个姿态方向纸面检查”；周二 C++ 专项“Eigen 数据表达与四元数转换”（转换示例由 Codex 给出）；周三任务 A“姿态导数模块交付”（G，原定本人 I/H 目标未达）；周三任务 B“四个最小输入的导数预测”；周三 C++ 专项“函数与模块边界”（经讲解后本人确认理解，整体 H，独立解释证据见记录）；周四任务 A“`runAttitudeCase` 循环功能”（H/G 混合来源，见记录）；周四任务 B“静止与正偏航两个正式演示工况”（运行通过，本人作出结果判断）；周四 C++ 专项“结果容器与生命周期”（H）；第 155 题本人报告已 AC、自评 C（代码与用时未提供）
- 计划偏差：周三任务 A 经本人决定按 G 来源结项，原定本人 I/H 核心实现目标仍未达，不安排机械重写；周四任务 A 的成品含 Codex 编写的单步处理与时间修正，不记为本人独立完成整段循环；顺延：周二算法及后续工况任务
- 主动跳过：暂无新增；第五周已明确跳过项不重开

## 2. 每日记录

| 原任务日期 | 实际执行日 | 完成内容 | 未完成及原因 |
|---|---|---|---|
| 09-14 | 2026-09-20 | 完成任务 A、任务 B 与 C++ 专项：概念阶次、姿态实验数据流、接口声明及参数传递 | 无；新接口尚未写入项目文件，按计划留到后续实现任务 |
| 09-15 | 2026-09-20 至 2026-09-21 | 完成任务 A：从固定轴小旋转、轴角四元数半角和增量姿态连接到四元数导数，并区分增量复合与导数积分两条路线；完成任务 B：预测并核对三种方向，正滚转的 East 分量最终判断为正；完成 C++ 专项的类型/API 学习，双向转换示例由 Codex 给出 | 算法尚未开始；双向转换未由本人手写 |
| 09-16 | 2026-09-21 至 2026-09-22 | 本人给出四个最小输入的导数预测，正确判断 `.cross()` 的 `undefined reference` 属于链接阶段；Codex 给出 C++ 专项四项直接答案后，本人确认均已理解；姿态导数定向构建及 5 项检查通过；本人决定任务 A 按 G 来源结项 | 周三任务已结项，但任务 A 原定本人 I/H 目标未达；C++ 专项其余三项未另作本人独立解释；积分循环未开始 |
| 09-17 | 2026-09-22 | 任务 A：本人写出 RK4 回调调用、循环头并把返回移到循环外；Codex 写入单步代码并修正时间，完成定向检查。任务 B：Codex 接入正式演示和 CMake、运行静止及正偏航；本人判断静止符合预期并说明零角速度使姿态不变，确认正偏航合理。C++ 专项：本人回答容器大小、样本副本、按值返回和 lambda 按值捕获，经局部纠正后完成 H | 正偏航在本轮没有本人重新展开方向推导；完整 CTest 未运行 |
| 09-18 | | | |
| 09-19 | | | |

## 3. 来源与当前能力

| 内容 | I/H/G | 本人完成或解释了什么 | 仍需帮助 |
|---|---|---|---|
| 系统、控制器和闭环阶次的区别 | H | 独立指出高度与竖直速度共同构成高度通道状态，并指出误差方程阶次由最高阶导数决定；经讲解后继续区分不同对象的阶次 | 参数值与初始条件、静态控制器与动态控制器的区别由 Codex 详细解释；未另做独立复述检查 |
| 角速度与四元数导数 | H | 独立算出 $0.1\,\mathrm{s}$ 内转过 $\pi/20\,\mathrm{rad}=9^\circ$、半角 $4.5^\circ$，判断绕 $z$ 轴时 $x/y$ 分量为零，并指出角速度是外部输入且本周不计算力矩；经提示得到单位姿态下的 $\dot q$，随后正确说明 $\delta q$ 是步内相对旋转、机体系增量应右乘当前姿态 | 小角度近似到一般姿态导数、$q_{NB}$ 的坐标变换链，以及增量复合与 Euler/RK4 数值积分的区别由 Codex 详细解释；后续通过非恒等初态和方向检查继续巩固 |
| C++ 接口与数据组织 | H | 经讲解后指出角速度是持续输入、`rk4Step` 完成单步积分、`runAttitudeCase` 负责完整循环、整步归一化和轨迹保存；正确解释返回局部引用会产生悬空引用 | `double` 按值传递、三维角速度类型及 `const&` 细节经过纠正；最终正确声明由 Codex 给出，未进行最终手动重写 |
| Eigen 数据表达与四元数转换 | H/G | 本人提出用索引或尾段读取四元数分量，识别 `.dot()`、`.cross()` 可计算点积和叉积，并追问 `Quaterniond` 与反向转换的用途 | `.tail<3>()` 语法、`Quaterniond` 类型说明及 `[w,x,y,z]` 双向转换示例由 Codex 给出；本人未手写转换代码，也未编译验证 |
| 姿态导数实现 | G（按此来源结项） | 本人先写出 `w`/`qv` 读取、点积/叉积结构及四维装配思路，按提示纠正了分量下标；随后请求 Codex 直接给出计算和检查代码 | 系数、标量负号、输入检查、最终源码与构建接入由 Codex 给出；原定本人 I/H 核心实现目标未达 |
| 姿态导数最小输入预测 | I | 本人正确预测零角速度、正/负 $z$ 轴角速度及非单位范数状态的四维导数 | 后续仍需结合非恒等初态检查乘法顺序 |
| 周三 C++ 函数与模块边界 | H（理解确认） | 本人正确指出 `.cross()` 的 `undefined reference` 是链接阶段找不到实现；在 Codex 给出四项直接答案后，本人确认已理解全部内容 | 命名空间/头文件依赖、无内部成员变量、检查与计算位置及三类错误归因的完整说明由 Codex 给出，未另有本人独立解释证据 |
| 回调与积分循环 | H/G（功能完成） | 本人补出 lambda 中的三参数导数调用，写出整数索引循环头并把 `return` 放到循环外；经提示理解 RK4 返回新状态及先记原始范数偏差再归一化的顺序 | Codex 提供 lambda 骨架、单步计算与容器代码，并代修起点及样本时间；整段成品不能记为本人 I/H 独立实现，演示入口与 CMake 后由 Codex 在任务 B 接入 |
| 周四任务 B：静止与正偏航 | I/G（判断/程序） | 本人解释零角速度时姿态不变，并确认正偏航输出合理；此前周二已预测机头由 North 朝 East | 工况配置、正式演示入口、CMake 接入和运行由 Codex 完成；本轮未由本人重新独立推导正偏航方向或编写工况代码 |
| 周四 C++ 结果容器与生命周期 | H | 本人正确回答 `reserve` 后大小为 0、保存初态后为 1；判断旧样本不会因 `state` 更新而变；指出返回容器按值、lambda 按值捕获角速度 | Codex 纠正“`state` 每循环运行一次”的理由：它是同一变量反复更新，样本持有当时的副本；并补充 lambda 自身持有捕获副本且 `DerivativeFunction` 在同步循环期间有效，未做独立新情境检查 |
| 归一化及旋转方向解释 | G（归一化代码）/待验收（方向） | 本人指出先保存原始范数偏差再归一化，经过纠正区分 Eigen 构造顺序与 `.coeffs()` 顺序 | `StateVector` 与 `Quaterniond` 双向转换及归一化调用由 Codex 写入；旋转方向的轨迹解释尚未进行 |
| 辅助检查、图表与文档 | G（定向检查） | 阅读并核对两个单位姿态输入与后续变号、非单位范数输入的预期 | 5 项 GTest 由 Codex 编写并运行；周四任务 A 的临时检查与任务 B 的正式演示构建运行由 Codex 完成；其余两个工况、图表和 README 尚未开始 |

### 周一任务 A 记录

- 本人初始判断：只知道高度或只知道速度都不足以确定后续高度，高度通道至少需要高度和竖直速度；误差微分方程的阶次由最高阶导数决定；完整无人机还包含更多动态环节。
- 提示与纠正：动态状态是预测未来所需、会随时间演化的内部记忆；固定的质量与增益是参数而不是状态；当前 PD 直接读取误差和竖直速度，没有内部动态状态，属于零阶动态控制器；积分累计量会引入一个控制器状态。
- 统一关系：对同一最小动态对象，微分方程最高阶数与独立标量动态状态数相对应；被控对象、控制器和闭环系统的阶次必须分别说明。
- 理想质量匹配、固定参考且未饱和时，闭环误差方程为 `e_ddot + kd*e_dot + kp*e = 0`。
- 来源边界：本人初始回答与判断记为 I；Codex 对控制器动态状态、参数/初值及不同阶次对象的详细解释记为 G；任务整体按提示后完成记为 H。
- 验证边界：本任务为概念整理，没有修改项目源码、运行构建或测试；未安排闭卷补考。

### 周一任务 B 记录

- 本人初始理解：正确识别当前核心是四元数积分，并指出轨迹容器保存每一步结果；最初误把本周运动学实验理解为含期望姿态、推力和控制分配的姿态控制。
- 提示与纠正：本周只研究“给定初始姿态和恒定机体系角速度，积分姿态轨迹”，不含期望姿态、控制器、电机、推力或力矩；`rk4Step` 内部四次调用导数函数并只推进一个时间步，`runAttitudeCase` 负责 200 步循环、整步归一化和轨迹保存。
- 数据边界：工况对象持有初始姿态、恒定角速度、步长和步数；运行函数持有当前状态和结果容器；积分器及导数函数临时借用所需数据并返回新值。
- 本人最终回答：持续输入为当前给定角速度；一次 `rk4Step` 完成四元数的一个积分步；完整循环、整步归一化和轨迹保存均由 `runAttitudeCase` 负责。
- 来源边界：初始识别和最终回答记为 I；Codex 对运动学/控制边界、调用方向及数据所有权的详细说明记为 G；任务整体按提示后完成记为 H。
- 验证边界：本任务只完成纸面数据流与职责划分，没有实现新模块、构建或运行测试。

### 周一 C++ 专项记录

- 本人初始声明正确选择四元数参数为 `const StateVector&`，并正确判断函数声明放头文件、函数定义/输入检查/数学计算放 `.cpp`。
- 需要纠正：将小标量 `timeS` 写成 `const double&`；将三维角速度误写成标量；后续重写中遗漏 `Eigen::Vector3d` 的引用符号，并出现 `StateVector` 拼写错误。
- 本人正确解释：不能返回指向函数局部结果的引用，否则函数返回后形成悬空引用。
- 最终接口边界：`timeS` 按值传递，四元数和三维角速度按 `const&` 只读借用，四元数导数按值返回；本人选择不再重复输入，按计划不追加闭卷或机械抄写。
- 来源边界：本人初始接口尝试、文件职责判断和生命周期解释记为 I；Codex 的参数传递纠正及最终完整声明记为 G；专项整体按提示后完成记为 H。
- 验证边界：未把接口写入项目文件，未编译或运行测试。

### 周二任务 A 记录

- 本人纸面结果：对 $\boldsymbol{\omega}_B=[0,0,\pi/2]\,\mathrm{rad/s}$、$\Delta t=0.1\,\mathrm{s}$，算出 $\delta\theta=\pi/20\,\mathrm{rad}=9^\circ$、四元数半角为 $4.5^\circ$；判断增量四元数的 $x/y$ 分量为零、$w/z$ 分量非零；明确本周角速度由外部给定，不由力矩和惯量计算，本周也不计算力矩。
- 小角度连接：在提示并纠正一次符号后，得到单位姿态下仅有 $z$ 轴角速度时 $\dot q=[0,0,0,\omega_z/2]$，并推广为单位姿态下 $\dot q=[0,\omega_x/2,\omega_y/2,\omega_z/2]$。
- 本人后续理解：正确指出 $\delta q$ 是当前时间步的相对旋转；在步内角速度保持不变时，可由 $\boldsymbol{\omega}_B\Delta t$ 构造 $\delta q$，再以 Hamilton 乘法右乘当前 $q_{NB}$ 得到下一姿态；也正确判断机体系角速度对应右乘增量。
- 提示与纠正：Codex 详细解释了 $q_{NB}$ 同时表示机体相对 NED 的姿态和 $B\to N$ 旋转算子、$q_{NB}(t+\Delta t)=q_{NB}(t)\otimes q_{B(t)B(t+\Delta t)}$ 的坐标变换链、由小角度极限得到 $\dot q=\frac12(q\otimes[0,\boldsymbol\omega_B])$，以及增量四元数复合、Euler 数值积分和 RK4 的关系。
- 来源边界：本人独立数值计算、输入/力矩边界、增量四元数链路及右乘判断记为 I；一般公式推导和表示/积分方法辨析由 Codex 详细讲解记为 G；任务整体按提示后完成记为 H。
- 验证边界：本任务为纸面推导和概念辨析，没有修改项目源码，也没有运行构建或测试；姿态导数实现、积分循环和固定工况仍未开始。

### 周二任务 B 记录

- 本人用几何想象作方向判断：零角速度时姿态不变；单位姿态绕正 Down 轴转动时机头由 North 朝 East；正滚转时机体系 $-z$ 方向的 NED East 分量为正。第三项在首次消息中写成“负”，本人随后明确说明是口误并更正为“正”；不将这次口误记为已确认的概念错误。
- 公式核对：单位姿态时 FRD 与 NED 轴对齐，正滚转角速度沿 $+x$；对 $\boldsymbol v_B=-\boldsymbol e_z$，初始变化方向为 $\boldsymbol\omega\times\boldsymbol v=(+\boldsymbol e_x)\times(-\boldsymbol e_z)=+\boldsymbol e_y$，即 East 分量为正。等价地，$R_x(\phi)[0,0,-1]^\mathsf T=[0,\sin\phi,-\cos\phi]^\mathsf T$，在小的正滚转角下 East 分量为正。
- 来源边界：三个方向的最终判断由本人给出，第三项的口误及后续澄清如实保留；叉积与旋转矩阵核对由 Codex 给出，记为 G；任务整体保留 H，不将 Codex 的公式核对记为本人独立推导。
- 验证边界：仅完成纸面预测与公式核对，未实现姿态模块，也未运行构建或测试。

### 周二 C++ 专项记录

- 本人尝试：提出用循环或 `state(1)` / 尾段读取四元数分量；指出点积和叉积可以直接使用 Eigen 的 `.dot()`、`.cross()` API，并追问 `Eigen::Quaterniond` 是什么以及为何需要反向转换。
- Codex 提示与示例：说明 `StateVector` 为动态维度向量，本任务四元数状态固定四维；给出 `quaternionWxyz(0)`、`.tail<3>()`、点积/叉积 API 及 `[w,x,y,z]` 与 `Eigen::Quaterniond` 的双向转换示例。明确转换只是类型适配，原样转回 `back` 仅为练习；导数函数不需要先转成 `Quaterniond`。
- 来源边界：本人提出的读取思路和 API 判断记为 I，语法纠正后的理解记为 H；双向转换代码由 Codex 给出，记为 G，不计为本人手写实现。不要求机械抄写示例。
- 验证边界：未修改项目源码，也未运行构建或测试。

### 周三任务 B 记录

- 本人预测：`q=[1,0,0,0]`、`omega_B=[0,0,0]` 时 `q_dot=[0,0,0,0]`；`omega_B=[0,0,2]` 时 `q_dot=[0,0,0,1]`；将角速度改为 `[0,0,-2]` 时 `q_dot=[0,0,0,-1]`；对非单位范数状态 `q=[2,0,0,0]` 和 `omega_B=[0,0,2]`，`q_dot=[0,0,0,2]`。最后一项回复的末尾方括号笔误不影响数值判断。
- 来源边界：四个结果由本人给出，预测记为 I；正式姿态导数的数学计算、输入检查、头文件、构建接入及定向测试由 Codex 提供，源码记为 G。本人先前的局部代码尝试如实保留，不将现有 G 源码升级为 I/H。
- 运行证据：现有 Windows MinGW 预设下，`aerial_core_gtest` 定向构建成功，`AttitudeKinematicsTest.*` 的 5 项检查通过；没有运行完整 CTest，也没有实现积分循环或四个固定轨迹工况。

### 周三 C++ 专项记录

- 本人判断：正确指出缺少 `<Eigen/Geometry>` 时出现的 `undefined reference to ...cross(...)` 属于链接错误，即链接时找不到 `.cross()` 的实现；经提示纠正本周角速度由工况外部给定，并追问 RK4 整步归一化后如何进入下一步求导。
- Codex 说明：`attitude_kinematics.hpp` 在 `aerial_control` 命名空间声明接口，`.cpp` 在同一命名空间定义；头文件通过 `integrators.hpp` 获得 `StateVector`，实现文件包含 `<Eigen/Geometry>` 才能使用 `.cross()`，并在读取分量前完成输入检查。导数函数的输入由参数提供，没有跨调用保存的内部状态，因此不需要类成员变量。
- 来源与边界：链接阶段判断由本人给出；其余模块与状态边界经 Codex 解释，专项整体记 H，不记为独立写出或独立复述全部内容。现有姿态导数源码仍为 G。

### 2026-09-21 勘误：周三 C++ 专项完成状态

- 上一段将专项“整体记 H”并列为已完成，结论过早。本人只明确判断了本次 `.cross()` 报错处于链接阶段；命名空间和头文件依赖、纯导数函数为何无需成员变量、检查与计算的代码位置，主要由 Codex 讲解，尚未确认本人能结合源码说明。
- 把报错归为链接阶段，不等于已经能区分缺少声明、缺少定义与源文件未接入构建三类原因。该专项当前状态改为“进行中”，保留已发生的回答和讲解，不补写未进行的验收。

### 2026-09-22 周三 C++ 专项后续确认

- Codex 随后按当前 `.hpp`、`.cpp` 和 `CMakeLists.txt` 直接给出四项答案；本人明确回复“我全部会了”。按经讲解后本人确认理解的口径，专项现记为完成 H，不安排机械抄写或额外闭卷补考。
- 证据边界：本人独立回答过 `.cross()` 的链接阶段判断；其余完整解释由 Codex 直接给出，尚无本人独立复述或新情境归因的证据。此前 2026-09-21 的勘误仍保留，用于说明当时为何不能提前判定完成。

### 2026-09-22 周三任务 A 结项决定

- 本人明确要求“将周三任务记录为完成”。据此，周三任务 A 按现有正式姿态导数模块的 G 来源结项；周三任务 B 与 C++ 专项维持原记录，不要求重做或机械抄写。
- 结项仅表示本轮工程功能已落地并完成定向验证，不改变任务 A 原定的本人 I/H 实现目标未达这一事实。积分循环、四个轨迹工况和完整 CTest 均未在本次结项中完成或重新运行。

### 2026-09-22 周四任务 A 记录

- 本人完成与帮助边界：本人在提示后写出 `attitudeKinematicsDerivative(timeS, state, angularRateBodyRadps)` 的 lambda 转发，提出当前状态与 RK4 单步调用，写出 200 步整数索引循环头，并把 `return samples` 移到循环外。Codex 将先前讨论的工况/样本结构、单步 RK4、原始范数偏差、`Quaterniond` 转换、整步归一化和样本保存写入 `.cpp`，再应本人请求修正时间计算。功能成品为 H/G 混合来源，不把整段循环升级为本人 I/H 实现。
- 执行顺序：初态时间为 `0`；第 `i` 步以 `i * dtS` 求导；整步后先记录 `abs(rawNextState.norm() - 1)`，再归一化并按 `[w,x,y,z]` 更新状态；下一样本时间为 `(i + 1) * dtS`。初态样本中的 `rawNormError=0` 表示它没有经历 RK4 整步。
- 定向运行证据：Codex 在现有 MinGW/Eigen 环境下直接编译新 `.cpp` 与所需核心源文件，用临时检查程序运行静止、正偏航两组 `dtS=0.01`、200 步输入；两组均为 201 个样本、末时间 `2 s`，全轨迹最大时间误差 `0`、归一化后范数误差 `0`、符号不敏感四元数误差约 `1.56e-12`。这是临时定向检查，不是新增 CMake/CTest 目标，也不是四工况验收或本人对方向结果的解释。
- 待办边界：`attitude_kinematics_demo.cpp` 尚无可执行入口，尚未接入 CMake；周四任务 B 的两个工况仍待在正式演示中运行和解释。完整 CTest 未运行，周五另两个工况未实现。

### 2026-09-22 周四任务 B 记录

- Codex 在 `attitude_kinematics_demo.cpp` 添加静止、正偏航两个固定工况与结果输出，并把可执行目标接入 `CMakeLists.txt`；用 `windows-mingw-gcc-debug` 预设重新配置、构建 `attitude_kinematics_demo` 成功，程序运行退出码为 `0`。这是 Codex 的 G 工程接入，不改变任务 A 的 H/G 来源。
- 正式演示两组均为 200 步、201 个样本、末时间 `2 s`。静止工况末四元数为 `[1,0,0,0]`，机体前轴的 NED 方向为 `[1,0,0]`；正偏航工况末四元数约为 `[0.707106781187648,0,0,0.707106781185447]`，前轴方向约为 `[3.11e-12,1,0]`，数值上指向 East。
- 本人判断静止结果符合预期，理由是角速度为零、姿态不变化；对正偏航答复“也合理”。正偏航 North→East 的纸面预测已在周二任务 B 完成，本轮没有另作本人独立推导。两个工况的正式程序运行已完成，任务 B 按上述解释证据结项；全轨迹指标、周五另两个工况及完整 CTest 仍待后续。

### 2026-09-22 周四 C++ 专项记录

- 本人正确判断 `samples.reserve(201)` 只预留容量、`size()` 仍为 0，保存初态后大小为 1；判断后续更新 `state` 不会改变已保存的初态，且函数按值返回容器后调用者可以继续读取；判断 `[angularRateBodyRadps]` 为按值捕获。
- 需要纠正的理由：循环内更新的是同一个局部 `state`，`push_back` 为每个样本保存当时的 `StateVector` 副本；lambda 拥有捕获的角速度副本，存于在 200 步同步调用期间有效的 `DerivativeFunction` 中。本人最初将样本不变归因于“state 一个循环运行一次”，将捕获有效归因于“都在一个函数体内”，经上述局部说明修正。专项按提示后完成记 H，不补写为独立解释完整生命周期。

## 4. 实验结果

| 工况 | 最大姿态误差 | 最大归一化后范数误差 | 方向检查 |
|---|---|---|---|
| 静止 | 待正式汇总 | 待正式汇总 | 末四元数 `[1,0,0,0]`，前轴 North；本人解释零角速度姿态不变 |
| 正偏航 $90^\circ$ | 待正式汇总 | 待正式汇总 | 末四元数约 `[0.7071,0,0,0.7071]`，前轴 East；本人确认合理 |
| 正滚转 $10^\circ$ | | | |
| 非恒等初态再滚转 | | | |

- 步长 / 积分次数 / 样本数：周四任务 B 正式演示的静止、正偏航工况均为 `0.01 s` / `200` / `201`
- 归一化前范数偏差：已逐步存入 `rawNormError`；最大值尚未汇总
- 一个错误及其定位过程：首次使用旧 `build-ninja` 树时，CMake 缓存指向迁移前路径；改用路径正确的现有 Windows MinGW 预设后，首次链接发现 `.cross()` 缺少 `<Eigen/Geometry>`，补充头文件后构建通过
- 实际构建与验证结果：此前 `aerial_core_gtest` 定向构建成功，`AttitudeKinematicsTest.*` 运行 5/5 通过；新 `.cpp` 的临时两工况检查通过；本次重新配置并构建 `attitude_kinematics_demo` 成功，静止与正偏航正式演示运行成功；未运行完整 CTest
- 尚未验证的部分：姿态导数源码来源为 G，未取得本人 I/H 核心实现证据；循环成品含 G 代码；正滚转及非恒等初态工况、全轨迹指标、完整 CTest 尚未验收

## 5. 算法

| 题号 | 完成情况 | 本人等级 | 是否需要后续帮助 |
|---|---|---|---|
| 20 | 已给出本人自评；代码与 AC 状态未报告 | C | |
| 155 | 本人报告已 AC；代码与用时未提供 | C | |

## 6. 下周入口

- 当前 commit 与未提交修改：待周末记录
- 本周真正理解的新概念：不同对象都可谈阶次；对同一最小动态对象，微分方程阶次与独立动态状态数相对应；机体系角速度既可构造步内增量四元数并右乘当前姿态，也可先映射为 $\dot q$ 再由数值积分器推进姿态
- 仍模糊的控制或 C++ 问题：后续结合实际模块继续巩固“参数、输入与内部动态状态”的边界，并通过非恒等初始姿态和方向检查巩固四元数乘法顺序
- 是否具备进入角速度动力学的条件：尚未验收
- 下一步：从周五任务 A 正滚转工况接续，再处理非恒等初态工况。周三任务 A 的 G 来源及周四任务 A 的 H/G 混合来源继续如实保留；第 20 题保留本人自评 C，代码与 AC 状态未报告，不安排重做
